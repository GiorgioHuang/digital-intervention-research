# UI_INVENTORY

> 需要设计的界面清单。口径：Doc 20 v1.3（Conceptual Prototype UX & Design System，330 节）为规范来源；本文件把它折算成**可交付的设计单元**，并如实标注每项当前的实现状态。概念研究阶段（ADR-061/062）：所有界面以合成数据与模拟服务演示，不代表在运行的服务；同意/审批屏是**被建模的未来系统**的 UX 模型，不构成当前研究的门（Doc 20 §3.2）。

## 汇总

| 类别 | 数量 | 说明 |
|---|---:|---|
| A. 设计系统基座 | **9** | 令牌与全局规则（Doc 20 §310–320、§285–307） |
| B. 参与者工作区 | **19** | 已实现 7 / 未实现 12 |
| C. 研究者工作区 | **17** | 已实现 5（局部）/ 未实现 12 |
| D. 支持者工作区 | **6** | 已实现 3 / 未实现 3 |
| E. 审核（Moderator）工作区 | **6** | 已实现 1 / 未实现 5 |
| F. 安全（Safety）工作区 | **6** | 已实现 1 / 未实现 5 |
| G. 管理（Administration）工作区 | **7** | 全部未实现 |
| H. 公共与邀请surface | **5** | 已实现 1（开发登录桩）/ 未实现 4 |
| I. 跨界面状态与共用组件 | **18** | 部分散落已实现，无统一规范 |
| **合计需设计单元** | **93** | 其中 **19** 项已有可运行实现（需视觉与交互重设计），**74** 项为 Doc 20 已规范、尚未实现 |

> 「已实现」= 仓库中有可运行 React 组件且有测试。**「仅 76 行基础 CSS」已过时**（2026-08-04 核对）：`apps/web/src/styles.css` 现为 839 行、192 个语义令牌，浅色/深色/高对比/强制颜色四套，A 节因此多数已定义——仍然需要的是**视觉与交互设计**（版式、组件外观、密度分档的实际运用），而不是从零建立令牌体系。

---

## A. 设计系统基座（9）

> **本节状态于 2026-08-04 按 `apps/web/src/styles.css` 逐条核对后更正。** 此前多行写着「未定义」，而令牌其实早已存在（该文件 839 行、192 个自定义属性）——把已经做完的事标成没做，会让设计代理去重建已有的东西，也把仓库的实际状态说小了。这与 C14/C17 那种「说大了」是同一类文档缺陷，方向相反。

| # | 单元 | 文档 | 状态 |
|---|---|---|---|
| A1 | 颜色令牌（surface/text/border/action/focus/info/success/warning/danger/Safety/moderation/AI/disabled） | §311–312 | ✅ 已定义：十三族语义色令牌，**浅色/深色/`prefers-contrast: more`/`forced-colors` 四套**，每个前景色旁注明实测对比度（如 `--color-text-primary` 17.60:1、`--color-safety-fg` 11.40:1）。Safety 用蓝色见 D-8 |
| A2 | 排版令牌与排版规则（可 200% 缩放、行高、行宽） | §313–314 | ✅ 已定义：七级字号（模块化音阶）、四级行高、四级字重、三档行宽（`--measure-narrow/default/wide`），并有 `--scale-font` 全局缩放钩子 |
| A3 | 间距令牌（Researcher 密集 / 标准 / Participant 宽松三档同源） | §315 | ✅ 已定义：`--space-0…9` 全部乘以 `--density`，三档密度由同一组令牌派生，正是本行要求的「同源」 |
| A4 | 形状与描边令牌 | §316 | ✅ 已定义：`--radius-0…3` + `--radius-pill`；`--border-hairline/default/strong/emphasis` |
| A5 | 焦点令牌（高对比、足够粗、偏移） | §317 | ✅ 已定义：3px 环 + 2px 偏移 + 2px 白色光晕（`--color-focus-ring` 与任一 surface ≥13.51:1），并有 `--focus-ring-total` 供预留空间 |
| A6 | 动效令牌与 reduced-motion 替代 | §318 | ✅ 已定义：四档时长 + 三条缓动曲线，`prefers-reduced-motion: reduce` 下整体降为 0ms |
| A7 | 层级/elevation 令牌（不得表示科学置信度或权威） | §319 | ✅ 已定义：`--elevation-0…3` 与 `--layer-base…live` 六级堆叠序；深色模式下阴影改以描边为主，避免深底上「浮起来」读成强调 |
| A8 | 图标体系 | §320 | ✅ 已定义令牌（`--icon-size-1…3`、`--icon-stroke`、`--icon-gap`），高对比下描边加粗到 2.5。**未做**：图标集本身——当前状态呈现用的是文字符号（◌ ○ △ ⬡ ⬟），且按 §B.1「颜色不得是唯一状态指示」，图标与文字始终并存 |
| A9 | 响应式与断点体系（桌面/平板/移动，共享设备隐私） | §301–307 | ✅ 已实现（2026-08-05）：断点（40rem/56rem，另有 `max-height: 30rem` 的横屏矮屏处理）与打印样式；共享设备模式由登录页的显式复选框开启（**不探测**），开启后：上下文横幅恒显当前身份、隐私屏「遮住屏幕」（不透明色块，并把身后一切设为 `inert`）、「切换使用者」清空本次到访、偏好改存 `sessionStorage`、空闲 5/7 分钟（见 I14）。`<title>` 已改为不含角色的固定标题。**量出来的两处修正**：横幅第一版在 320px 下占 304px（视口 36%，超 §D.5 的 25% 上限），改为两行并排按钮后 169–197px（20–23%）；放大字号（xl/xxl）下仍达 45%，故在该字号取消粘性——选择放大字号的人正是屏幕空间最紧的人。**未做**：列表页减少内容预览一项无需实现，会话列表本来就不显示消息摘要；浏览器通知不存在，无内容可省 |

## B. 参与者工作区（19 + 1 项实施中新增）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| B1 | 首页（任务式，非信息流） | §107、§36 | ✅ 已实现（含「你在研究里的位置」与「等你决定的事」两个区块）|
| B2 | 我的同意选择（细粒度、可撤回） | §95–102 | ✅ 已实现（2026-08-06 补：**同意文本改版后要求重新同意**。`ReConsentRequired` 是权限引擎一直会读、会当场停掉该范围一切访问的一个同意状态，而平台没有任何一处能写它——文本可以改版，参与者继续挂在一份对已不存在措辞的同意上，屏上还写着「已同意」。现在该状态会以现在时呈现：「已经停了，不是将要停」，并显示**改了什么**（必填，由记录的人写），以及拒绝不牵连其他选择与研究中的位置，D-33） |
| B3 | 消息：会话列表 | §157–158 | ✅ 已实现 （2026-08-06：会话现在也可以建立在**参与者批准的支持者关系**之上，此前只能建立在匹配得到的连接上——你能给陌生人写信、不能给自己批准的家人写信；「可以给我写信」是关系上一条独立的可授权动作，撤回关系会当场停掉会话，D-29。同日修正：这类会话此前把参与者自己批准的支持者显示成「A community member」——关系会话的另一侧是账号不是 participant，查参与者名册查不到就落到了那句为社区陌生人写的占位；而那句占位按设计对所有人相同，两位支持者会变成两行分不清谁是谁。现在模块不猜，名字在组合根用 M01 账号名解析；真解析不到就写明「名字缺失」，不拿描述填空，D-30） |
| B4 | 消息：会话详情与发送确认（双状态机、诚实投递态） | §158–162 | ✅ 已实现 |
| B5 | 认识新朋友（匹配开启、候选、决定、互相接受、建立连接） | §143–156 | ✅ 已实现（2026-08-06 补上退出：连接现在**可以解除**，一方即可，且明说这不是屏蔽——此前唯一的出口是屏蔽，一次平常的道别只能被装扮成一次指控。解除时该连接上的会话置为 `Expired`，因为消息命令本来就会在 basis 失效时拒绝，留成「进行中」等于让列表说谎；已写的内容不删，对方也不会被通知，D-26） |
| B6 | 社区（列表、版本化规则加入、时间序 feed、草稿-发布） | §134–141 | ✅ 已实现（2026-08-06 补上退出：社区现在**可以退出**——此前只能加入，「参与社区完全是自愿的」只在第一次为真。退出**不带同意前置**：加入受 `community-participation` 门控，退出若同样门控，撤回那份同意就会把人困在它带你进去的社区里；已发的帖子不删，屏上明说，D-26） |
| B7 | 帮助与安全（报告、屏蔽、安全信号） | §142、§166–168 | ✅ 已实现（含「你屏蔽过的人」列表与解除屏蔽——此前确认框承诺「随时可以解除」，却没有任何地方能看到或解除）|
| B8 | 我的研究（My Study） | §108 | 未实现 |
| B9 | 生命故事：档案首页 | §109–110 | 部分实现：可读取（此前没有任何读动作）、来源与证言状态逐条标注、已撤回条目对本人仍可读；可见性与导出未实现 |
| B10 | 生命故事：创建/AI 起草/确认为证言 | §111–118 | 部分实现：新建条目 + 就确切版本确认为证言（改动后确认不顺延）；修订既有条目未实现；AI 起草按 D-14 暂不启用 |
| B11 | 生命故事：可见性与受众选择 | §119–124 | 不实现，等有读者（D-16）：`visibility` 写得进去但没有任何读取路径会看它，`access_grants` 无命令可写入 |
| B12 | 生命故事：撤回与导出 | §125–126 | 部分实现：索取自己信息的副本 + 请求状态如实回报（不承诺投递、不问理由）；生命故事单条导出与撤回入口未实现 |
| B13 | 无障碍与偏好设置（能力自适应模式） | §103–104、§286–287 | 部分实现：**只上有真实 CSS 支撑的四项**——字号（四档）、内容密度（两档）、对比度（两档）、减少动效。这些钩子样式表里一直都有，却从来没有任何代码去设置它们，等于四档字号与三档密度**定义了却够不到**。改动**即时生效、没有保存按钮**（§B13 要求「当场看到，不用先保存」；有保存按钮就意味着它还没生效）。「标准」是**移除属性而不是写入 `standard`**，否则会悄悄推翻使用者在操作系统里已经做过的设置。措辞上**没有任何一项被描述为缺陷**。**刻意不上**：念出来、一次一步、更简单的说法、更长操作时间——设计里有，实现里一样都没有，摆上去就是记录一个没人会读的选择（同 D-2 的规矩）。「有人在旁边帮我」已由 D-15 的常驻横幅实现，本屏只指路不重复 |
| B14 | 公开档案（PublicProfile）编辑与预览 | §131–133 | 未实现（在此之前，对外显示的姓名一律取自参与者记录，见 D-12；社区页已如实说明这一点）|
| B15 | 评估（基线/随访） | §106、§172、§250 | 不实现（D-17）：`assessment.record` 无 owner 权限，参与者不能填；且平台无量表题库，题目是经批准的研究材料，不能由实现者发明 |
| B16 | 活动与互动准备/完成/反思 | §169–171 | 未实现 |
| B17 | 报告与屏蔽中心 | §168 | 部分实现：屏蔽的查看与解除已在 B7 内；举报历史仍未实现 |
| B18 | 暂停与退出（范围选择、后果摘要、回执） | §173–178 | 部分实现：退出已在首页常驻，附后果摘要（已锁定的数据集不会被改写）；暂停未实现——`Paused` 是合法状态但没有任何命令能转入 |
| B19 | AI 伴侣（入口、身份、来源、不确定性、拒绝、记忆控制） | §205–220 | 未实现（M11 网关已有，Level-5 全禁） |
| B20 | 谁能访问我（授权关系的批准与撤销） | Doc 4 关系模型；§95–102 的另一半 | ✅ 已实现（本清单原 19 项之外新增）：`relationship.approve` / `relationship.revoke` 一直是 ownerOnly，但**没有任何查询列出关系**，于是提议在等一个参与者根本看不到的批准，已生效的授权也无法被唯一有权终止的人终止。同意管「能对我的信息做什么」，这一屏管「由谁」，权限引擎两者都要 |

## C. 研究者工作区（17）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| C1 | 研究者仪表盘 | §37 | 未实现 |
| C2 | 研究项目与研究问题 | §57–58 | ✅ 局部（建项目/协议草稿） |
| C3 | 证据检索与结果卡（含冲突证据呈现） | §59–61、§60 | ✅ 局部：可检索知识平台并按「先出处、后论断」（§51）呈现结果（来源系统 · 标识 · 版本在标题与摘要之前）；可建证据评审、附引用、提交、由他人批准。**检索失败不写成「没有证据」**——上游 503 显示为错误，不显示空结果。**解析失败的引用不被渲染成引用**：解析不到时记录会以原始标识作标题、来源为 `unknown`，屏上逐条标注「找不到，未与任何来源核对过」。冲突证据的呈现已随 C4 实现。**2026-08-05：评审可以被退回**——此前这条队列只有「批准」一个出口，评审人若判定评审不合格便无事可做（D-25）；退回理由在自己的工作列表里逐条显示，并写明「什么都没有被删掉」 |
| C4 | 证据决定与快照 | §62–63 | ✅ 局部：可就某份评审写决定、由第二人同意，同意时在同一事务写入不可更改的 EvidenceSnapshot（内容哈希上屏）。**结果词表是平台的、不是界面发明的**——数据库拒绝词表之外的值，且「已批准／已拒绝／暂定」**不是**结果：结果回答「证据说什么」，批准是「谁同意了」，两者合一会让一份决定因为有人签了字而读起来像已成定论。**「证据相互冲突」是一等结果**（§60），措辞按发现来写，并与「证据不足」明确区分（一个是找到了且互相矛盾，一个是几乎没找到）。决定可以写在尚未批准的评审上——命令允许，因此界面**如实警告而不假装拦截**；批准屏同样标注「同意这份决定并不等于批准那份评审」。**2026-08-05：决定可以被拒绝**，且拒绝**不写快照**——快照是「已同意的决定依据了什么」的记录，被拒的决定没有可供引用的依据（D-25）；不同意的理由在自己的工作列表里显示 |
| C5 | 协议版本编辑器 | §64–65 | ✅ 局部；**2026-08-05：可以看到自己提交的版本后来怎么了**——此前没有任何查询列出研究者自己的协议版本，一版起草、提交，然后从每一块屏上消失，结局只能靠问人；拒绝理由存下来了却没有读者，等于把拒绝变成一次带标签的消失 |
| C6 | 协议审批视图（精确版本、职责分离：批准人≠提交人，ADR-051；**不是**双人批准） | §66、§89 | ✅ 局部（独立决策屏：队列即入口，自己提交的版本按钮不可点，内容哈希与版本号与控件同屏，决定前重读并在哈希/时间戳变动时拒绝执行）；**2026-08-05 补上「拒绝」**——此前整屏只有一个出口，清空队列的唯一办法是把里面的东西全部批准；拒绝与批准同权限键、同职责分离，理由必填并存在版本上（D-25） |
| C7 | 干预配置 | §67 | ✅ 局部（2026-08-06）：**干预组合（M06）此前在产品里完全看不见**——六条路由（建干预／加版本／提交／批准／启用／按项目配置）没有任何一处调用，而且 M06 **一个查询都没有**，所以哪怕直接打 API 建出来的干预，从那一刻起也再没人能看见：批准人手上有两个决定却无从得知有东西在等，提交了版本的研究者查不到它后来怎么样。对一个以长者数字干预为题的平台，干预恰好是唯一看不见的东西。现在研究者屏可建干预、加版本、提交（并明说批准与启用是别人的一步），批准人屏由列表驱动、每行只给当前状态接受的那一个控件，启用确认写明**顶掉哪一版**。**证据分级不显示**：`evidence_status`（E0–E5）与 `evidence_direction` 没有任何代码写过，全库都是默认值，印出来会被读成有人评过并给了最低档，屏上照实说没有地方能记录它（D-34）。**未做**：§67 的按项目绑定配置（`intervention-configurations` 仍无调用方）、C8 的 AI 配置与变更警告 |
| C8 | AI 干预配置与变更警告 | §68–69 | 未实现 |
| C9 | 社区配置 | §70 | 未实现 |
| C10 | 匹配/互相接受/连接策略配置 | §71 | 未实现 |
| C11 | 合成人物画像与情景设置 | §72 | 未实现 |
| C12 | 参与者列表与详情（研究者视角） | §73–74 | ✅ 局部（2026-08-05 修正）：入组队列改为**按状态驱动**。此前四个链条步骤与资格判定都是常开按钮，摆在一个「输入入组标识」的文本框旁边，而**状态在屏上任何地方都不显示**——四个按钮里只有一个能成功，是哪一个从屏上无从得知，在 `Invited` 上按「启动」得到的是一次正确但毫无用处的拒绝。**提供一个当前按不动的控件，与提供一个按了不做事的控件是同一个缺陷**，而且它教人把拒绝当成常态。现在每一条入组显示它走到哪里（用人话，不只是状态名）、只提供它当前状态允许的那一步、并说明那一步会把它移到哪个状态；资格判定只在 `Screening` 时出现，因为命令只从这个状态接受它；已撤回／已终止的不再提供撤回。**链条其余部分以文字保留**，并说明没提供不是被藏起来，而是还没轮到。每次动作后重新读列表——不重读的话，行上留着动作之前的状态，摆出来的正是刚刚失效的那个按钮 |
| C13 | 干预与评估监测 | §75–76 | 未实现 |
| C14 | 数据集定义/变量构建/生成/质量复核/锁定 | §78–83 | ✅ 局部（**整条链现在都可达**：写定义 → 他人批准 → 生成版本 → 记录质量复核完成 → 锁定。此前**只有最后一步有界面**，前四步没有任何人能执行，锁定队列因此永远不可能通过产品被填满——一个队列填不满的决策屏，等于从没被用过。定义的变量字典即「装什么进去」，消息正文默认排除，未列出的就不包含；批准屏把变量列在控件旁边，起草人不能批准自己写的定义（命令与数据库 CHECK 双重强制），行内在按钮之前就说明这一点。「记录质量复核完成」是人对自己行为的记录，不叫「批准」，也明说这不是锁定。独立锁定屏：manifest 哈希与控件同屏、确认时给全值；定义批准人与锁定人**可以相同**（D-11），界面明说这条规则并显示定义批准人） |
| C15 | 分析计划/运行/解释记录 | §84–87 | ✅ 局部：写计划 → 他人批准 → 记录运行（**只能针对已锁定的数据集版本**，选择器只列已锁定的并说明原因，而不是让人选完再被拒绝）→ 写解释 → 他人批准。**「记录运行」不是「执行分析」**：平台没有计算引擎，存的是人对「针对这一版数据跑了分析、产出是什么」的记录，按钮因此不叫「运行分析」。每次运行显示所针对数据集版本的 manifest 哈希——这正是先锁定再分析的意义：解释只针对一次运行，运行只针对那一份数据。**2026-08-05：分析计划可以被拒绝**（D-25）；拒绝理由在自己的工作列表里逐条显示 |
| C16 | 研究发现与干预决定 | §88–90 | ✅ 局部：研究发现可从**已批准的解释**中写出并由他人批准；批准屏把发现所依托的解释与运行标识列在控件旁。**只有 `finding.approve` 是 MFA 级**，因此强认证提示只出现在发现一节——在计划与解释上重复它会夸大那两者的代价，并教人略过唯一真实的那条提示。干预决定（§90 的 InterventionDecision）未实现。**2026-08-05：研究发现可以被拒绝**，且因与批准同权限键，拒绝同样要求强认证——拒绝不是决定里便宜的那一半（D-25）；拒绝理由在自己的工作列表里逐条显示 |
| C17 | 报告与受控导出 | §91 | ✅ 局部（**报告一侧现在也可达**：开报告 → 写版本 → 他人批准。此前本行名字里的「报告」没有任何界面——只有导出半边有屏，旁边的报告没有，于是报告版本的审批队列永远没有东西可批。批准即固定该版本的措辞：数据库触发器拒绝改动已批准版本的内容，更正只能是新版本，确认文案直说这一点；起草人不能批准自己写的版本（命令 + 数据库 CHECK），行内在按钮之前说明；`report.approve` 只要求确认、不是 MFA 级，屏上不谎称需要强认证。独立导出决策屏：批准与拒绝同权限键 `export.approve`，同样标注强认证、同样受职责分离约束；**批准之后的执行也已上屏**——「等待执行的导出」列出已批准／已生成／已交付三态，可生成包（显示 manifest 哈希）并记录交付与签收；措辞守住三态之别，且「记录我已交付」是**人对自己行为的记录**，平台并不发送任何东西。此前批准就是终点：没有任何查询列出已批准的请求，包永远不会被生成，索取自己信息的人会被如实告知「已同意」，然后再也没有下文；**队列里两类请求分开呈现**——研究导出是把别人的数据交给第三方，可携带性请求是本人索取自己的信息，后者「去标识化：无」不是问题而是要点，屏上同时说明数据库本就拒绝可识别的研究导出，并显示该请求已经被施加的限制） |

## D. 支持者工作区（6）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| D1 | 支持者首页 | §38 | ✅ 已实现（含「你支持的人」列表）。**2026-08-06 新增「对话」**：支持者可以读参与者开启的会话并回信（草稿→确认发送，与别处同规矩）。会话只能由参与者开启，支持者不能主动开。列表里对方的名字解析不到时写明「名字缺失」，不再套用「A community member」——支持者与他支持的人并不在同一个社区里（D-30） |
| D2 | 邀请与权限复核（可见权限） | §180–181 | 部分实现：可看到自己被写进的授权关系、关系状态与该关系允许做什么（不出现点号动作键）；**不报告对方的同意状态**——那是参与者的，把它显示给支持者等于让他推断出参与者没打算告诉他的事。邀请的接受/拒绝仍在参与者一侧（B20）。**2026-08-06：关系终于能在产品里被建立**——此前 `POST /v1/relationships` 全产品没有调用方，整条支持者路径只有能直接调 API 的人才能开启（D-28）。协调员屏可提议关系，明说提议不授予任何东西，且可授权动作只列平台真的由关系门控的那两个 |
| D3 | 生命故事贡献提交 | §182、§127 | ✅ 已实现 |
| D4 | 我的贡献状态列表（接受≠证言） | §128 | ✅ 已实现 |
| D5 | 共享活动支持 | §183 | 未实现 |
| D6 | 报告关切 / 访问被撤销状态 | §184–185 | 未实现 |

## E. 审核工作区（6）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| E1 | 审核仪表盘 | §39 | 未实现 |
| E2 | 报告队列（举报人身份不出现） | §186、§193 | ✅ 已实现 |
| E3 | 案件详情与证据最小化呈现 | §187–188 | 部分（2026-08-05）：队列行现在带上举报类别、举报描述、被举报对象与**是否指向一条内容**——最后一项决定哪些决定能作用，不带上它审核员只能靠被拒绝来发现。仍未做：内容本身的最小化呈现（只显示被举报的那一条及其必要上下文） |
| E4 | 审核决定与确认（不可变） | §189–190 | ✅ 局部（2026-08-05 重做）：此前十个结果全放在一个下拉里，旁边是「输入案件标识」的文本框，而**没有一个结果真的做任何事**——`moderation_decisions` 没人读，`post_state` 的 `Hidden`/`Removed`/`Restored` 写在 CHECK 里却从没被写入过，于是「移除内容」关掉案件、报告成功、帖子仍在社区里。现在改为**按案件驱动**：`Hide`/`Remove`/`Restore` 在同一事务里改帖子状态（社区流本来只显示 `Published`，所以这三个是真的），`Dismiss` 关案件；「警告」改写为「记录你在别处做过的事」并明说平台不发送任何东西；**限制/暂停/断开/封禁/升级五个不上屏**，并用一节点名它们为什么不在（D-23）。案件若不指向内容，作用于内容的决定不出现，屏上直说原因而不是让人被拒绝后才知道。**升级不再关案**：它曾把案件关成 `Actioned`，即「转给别人」和「已处理完」是同一个按钮 |
| E5 | 申诉与恢复 | §191–192 | 未实现 |
| E6 | 审核→安全联动 | §194 | 未实现 |

## F. 安全工作区（6）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| F1 | 安全仪表盘 | §40 | 未实现 |
| F2 | SafetySignal 队列 | §195 | ✅ 已实现 |
| F3 | 人工分诊视图 | §196–197 | ✅ 局部（2026-08-06：演示种子现在留一条待分诊信号——此前安全工作区里一条信号都没有过） |
| F4 | 关闭为非安全事件 / 转为安全事件 | §198–199 | ✅ 局部 |
| F5 | SafetyEvent 视图与安全动作 | §200–201 | ✅ 局部（2026-08-06）：此前安全事件**建了就到此为止**——`event_state` 默认 `Open` 且没有任何代码改得动它，没有查询列出它，没有屏幕显示它，而分诊屏说的「已转为安全事件」读起来像升级给了某个会处理它的东西。现在：事件可见（含来源信号的类别与严重度，**都是文字不是色阶**，§200）、可记录动作、可按状态机推进；**只可追加的时间线**把动作与状态推进放在一起，数据库触发器强制不可改不可删。措辞三条：平台自己不做任何事、「已解决」不代表人安全了、写做了什么不写说了什么。空状态说「空白不等于已判断」并提供「决定不需要采取动作」。**未做**：设计要求的「与干预的关联性」平台没有地方记录，屏上明说而非默默省略；动作负责人、监测排期、暂停/恢复（F6）仍未实现 |
| F6 | 暂停/恢复与紧急限制说明 | §202–204 | 未实现 |

## G. 管理工作区（7 + 1 项实施中新增）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| G1 | 管理仪表盘与系统状态 | §41 | 未实现 |
| G2 | 用户与组织 | §21 | 未实现 |
| G3 | 角色与服务账号 | §21 | 未实现 |
| G4 | 集成与 AI 供应商配置 | §21 | 未实现 |
| G5 | 作业与死信队列 | §21 | 未实现 |
| G6 | 功能旗标 | §21 | 未实现 |
| G7 | 审计访问 | §21 | ✅ 已实现（2026-08-06）：**平台有 61 处写审计，之前没有任何一处读**——`audit.view` 自权限目录写下之日起就授给了三个角色，而没有一行代码检查过它；那份记录只追加（触发器强制）、任何人都看不到。这是本轮清查里最大的一处：记录本身完整、正确、fail-closed，而没有一个人能看见。**屏上先说这份记录里没有什么**：（1）**不记录任何人的查看**——看参与者档案、开会话、跑报表，一律不写，所以这块屏答不了「谁看过我的记录」；（2）**不记录拒绝**——`result` 的 CHECK 里有 `Allowed`／`Denied`，全代码库从没写过这两个值，权限引擎拒绝一次什么都不留下，来查未遂滥用的人会一无所获（D-32）。空结果处再说一次「这不等于没有发生」。**读审计本身也被记录**（Doc 15 §21）：理由必填、连同筛选条件一起，在返回任何一行之前写入同一张表，写不进去就读不到；理由由不必填改回必填，因为 D-6 给自己写的解除条件（后端真的记录）已经补齐（D-31）。只显示「谁、对什么、做了什么、何时、凭什么权限」，不显示被操作资源的内容 |
| G8 | 紧急访问记录与事后复核（break-glass）+ 治理保留 | Doc 16 §38.5 | ✅ 局部（2026-08-05 新增行）：两条命令与待复核队列在服务端**一直都在，而没有任何调用方**——`break-glass/pending-review` 是一个既看不到、也无法通过产品填满的队列，而它恰好长在平台的问责路径上。现在可以记录一次紧急访问（MFA + 确认，理由／范围／预计结束时间都必填）、由**记录人以外的人**复核（正当／不正当／需要跟进），演示种子留一条待复核。**措辞按事实**：权限引擎从不读 `break_glass_records`，所以记录**不给任何访问权**、`expiresAt` 也不关掉任何东西，屏上第一段与到期时间旁边都明说（D-22）。**治理保留刻意不上屏**：没有任何一处读 `governance_holds`，放上保留冻不住任何东西，而放保留的人会因此停止去找别的办法拦（D-21）；屏上用一节说明它为什么不在这里 |

## H. 公共与邀请 surface（5）

| # | 界面 | 文档 | 状态 |
|---|---|---|---|
| H1 | 登录/身份入口（当前为 dev-header 桩 + 访问口令横幅） | §22、ADR-104 | ✅ 已实现（桩） |
| H2 | 安全邀请落地页 | §22、§92 | 未实现 |
| H3 | 账户激活 | §22、§93 | 未实现 |
| H4 | 公开研究信息与支持联系 | §22 | 未实现 |
| H5 | 无障碍声明 | §22、§285 | 未实现 |

## I. 跨界面状态与共用组件（18）

| # | 单元 | 文档 | 状态 |
|---|---|---|---|
| I1 | 工作区切换与上下文横幅 | §23–24 | 部分 |
| I2 | 全局导航规则与角色感知导航 | §26–29 | 部分 |
| I3 | 受保护存在的呈现（不泄露存在性） | §27、ADR-050 | 已在后端强制，前端无统一呈现 |
| I4 | 状态徽章与生命周期/审批/资源状态呈现 | §43–45、§56 | 散落 |
| I5 | 可见性与受众预览组件 | §46–47、§261–262 | 未实现 |
| I6 | 出处与来源组件（Source before assertion） | §51、§264 | 未实现 |
| I7 | 认识论类型标签（Doc 19 §10 十类） | §52 | 未实现 |
| I8 | AI 参与标签与 Draft 呈现 | §53、§211 | 部分（消息草稿为人写草稿；AI 起草按 D-14 暂不启用，故当前无 AI 参与标签需求）|
| I9 | 版本呈现与当前/历史对比 | §54–55 | 未实现 |
| I10 | 投递状态呈现（诚实措辞） | §50、§161–162 | ✅ 已实现 |
| I11 | 加载/骨架/空/离线/同步/陈旧状态 | §224–229 | 部分（2026-08-04 核对更正，此前写「缺失」）：`StateBlock.tsx` 提供 `LoadingState` / `EmptyState` / `ErrorState` 三个共用组件，全平台在用，且守住一条规矩——**「还没有」与「还没加载」是两个事实，必须读起来不同**；一片空白若含义是「正在加载」，那就是在说谎。每个状态都是**图标 + 文字**，颜色只作次要线索，因此在灰度、打印与色觉差异下同样成立。**未做**：骨架屏、离线/同步/陈旧三态（应用没有离线能力，画一个离线态等于承诺一件做不到的事） |
| I12 | 版本冲突呈现 | §230 | 部分：审批决策屏在执行前重读对象，若内容哈希或时间戳变动即拒绝执行并说明（C6）；**未做**：通用的版本冲突组件 |
| I13 | 错误状态与严重度分级（可恢复/阻断/安全关键/安全性关键） | §231–237 | ✅ 已实现（2026-08-04 核对更正，此前写「部分（原始错误码）」）：`errors.ts` 把服务器错误码译成四段结构（标题 / 安抚 / 原因 / 下一步）并分三级严重度，映射到 warning / danger / Safety 三个语义族；原始错误码收进 `<details>`，因为它只对支持人员有用。参与者侧与员工侧措辞分开（`presentError` 与 `staffLoadError` / `staffActionError`）——**读取失败与命令被拒不是同一回事**，把「职责分离」当成读取失败的原因曾是一个真实缺陷 |
| I14 | 会话超时与共享设备超时 | §238–239 | ✅ 已实现（2026-08-05）：普通 20/25 分钟、共享设备 5/7 分钟；倒计时是文字并带 `role="timer"`，整半分钟通过独立的 `aria-live="polite"` 播报（每秒播报会把其他一切埋掉）；共享设备最多延长一次，普通模式不限次；超时后回到登录页并显示中性说明（只说计时到了，不说屏幕上原本是什么）。**三处按事实偏离设计稿**：（1）不说「已保存成草稿」——平台没有草稿（D-20）；（2）做对话框不做粘性条（D-19）；（3）「空闲」只认按下与按键，不认指针移动——袖子压在触控板上不该等于「人还在」 |
| I15 | 确认模式族（简单/详细/step-up/双人批准/撤销） | §240–246 | 部分（已有确认对话框） |
| I16 | 表单族（敏感字段、多步表单） | §247–249 | 部分 |
| I17 | 数据展示族（表格、时间线、图表、卡片家族） | §251–259 | 未实现 |
| I18 | 通知策略与隐私边界 | §271–276 | 未实现 |

---

## 优先级建议（供设计代理排期）

1. **P0 — 让已实现的界面可用且合规**（2026-08-05 更新）：A1–A9 的**令牌已定义**、I11/I13 的**共用组件已实现**，因此 P0 不是「建立基座」而是**把基座用起来**。已完成：三档密度与四档字号的实际入口（B13 阅读与显示偏好，此前这些令牌定义了却没有任何代码去设它们）、A9 共享设备隐私与 I14 会话超时。**P0 余下**：图标集（A8 令牌已有、图标本身没有）。这仍是唯一能立刻落到已部署原型上的部分。
2. **P1 — 补齐后端已具备、前端缺失的高价值流程**（2026-08-04 更新：**本条已基本走完**）：B9–B12、C3–C4、C14、C15–C16、C17 均已部分实现，演示种子也已把这些链路种上数据；B15（评估）按 D-17 阻塞，B11 按 D-16 不实现。剩余的是 C7–C11、C13 与 E/F 两个工作区。
3. **P2 — 概念完整性所需**：其余研究者/审核/安全流程、管理工作区、公共 surface、AI 伴侣（受 Level-5 禁止约束）。
