# DESIGN_DECISIONS

> 设计阶段未决项的裁定记录。裁定人：GiorgioHuang（项目所有者），2026-08-02。四份设计规范中标为「需要产品决策」的条目以本文件为准；规范正文与本文件冲突时，以本文件为准并应回改规范。

| # | 议题 | 裁定 | 实施后果 | 来源 |
|---|---|---|---|---|
| D-1 | 协议批准是否双人 | **不要双人** | 维持现状：仅执行职责分离（批准人≠提交人，ADR-051 + DB CHECK）。审批屏不得出现「等待第二位批准人」类文案；UI_INVENTORY C6 已改正 | RESEARCHER U1 |
| D-2 | 22 个同意范围如何呈现 | **一次全上（部分实施：目前 6 项）** | 裁定不变：同意屏一次呈现全部范围，不按功能分批，规避「在被功能吸引的时刻做同意决定」的不当影响。**但 22 项现在不能诚实地摆上去**：平台实际执行的只有 6 个 scope（`study-participation`、`community-participation`、`open-matching`、`participant-messaging`、`supporter-involvement`、`supporter-contribution`——每一个都在 `packages/policy/src/catalogue.ts` 里作为某个动作的 `consentScopes` 前置条件出现）。`consent_current.consent_scope` 没有 CHECK 约束，任何字符串都能写进去，所以再加 16 个开关只会产生「记录了决定但没有任何检查会读它」的控件——那不是控制权，是平台并不提供的保护承诺，与 D-5 立下的同一条规矩相抵触。**已做**：6 项全部上屏（此前只有 4 项，缺的两项恰好是决定「支持者能看到什么、能做什么」的那两项），每项写明它实际管住什么，当前状态从服务器读取（与权限引擎同一张投影表 `consent_current`），并声明这些是平台当前真正执行的选择。**解锁条件**：先让其余 scope 在权限目录里各自门控某个动作，届时再逐项上屏。 | PARTICIPANT C3 |
| D-3 | 移动端导航形态 | **底部固定栏** | 拇指可达优先。约束：底部栏不得遮挡表单提交区与确认对话框；需为 `env(safe-area-inset-bottom)` 留白；条目数超过 5 个的工作区（研究者 17 个目的地）不适用底部栏，改用顶部分组导航 | DESIGN_SYSTEM / PARTICIPANT |
| D-4 | block/report 是否强制共享上下文 | **不强制** | 保留自由发起路径——「线下认识、想预先屏蔽」是真实场景，强制上下文会挡掉它。但**输入框不得是裸标识符**：需提供从上下文一键发起（主路径）+ 手动输入（备用路径），且手动路径的成功/失败反馈必须与标识是否存在无关（当前后端两张表均无外键，行为已一致，实施时不得引入外键或差异化文案） | PARTICIPANT C1 |
| D-5 | 导出文件的「合成数据」标记 | **等后端实现** | 在后端实际写入标记之前，**界面不得声称导出文件带有该标记**。批准确认文案暂只描述界面已知事实；标记实现后再补文案与测试 | RESEARCHER U7 |
| D-6 | 审计访问「理由」是否必填 | **不必填** | 前端不设必填。避免「前端必填、后端不记录」的假保障。若将来后端记录该字段，再评估必填 | RESEARCHER U4 |
| D-7 | 紧急联系渠道号码与服务名 | **先不显示** | 安全相关界面只保留「本平台不是紧急求助渠道」的事实陈述，**不列任何具体号码或机构名**（未经批准的号码比不给更危险）。同时不得显示值班响应承诺 | MODERATION U1/U2 |
| D-8 | Safety 语义色 | **蓝色** | 令牌已改：浅色 `#152A6B` / `#E9EDFA` / `#2B3F8F`（11.40:1、9.50:1），深色 `#BCC9F5` / `#131A33` / `#7186D6`（10.47:1、5.48:1），全部达标。**代价须知**：Safety 蓝与 info 蓝、action 蓝同属蓝族（safety-fg 与 info-fg 相互对比度仅 1.51:1），因此 Safety 的辨识**主要依赖 ⬡ 图标与「安全」字样**，颜色退为次要线索。这与「颜色不得是唯一状态指示」一致，但要求实施时 Safety 事务**永不省略图标与文字** | DESIGN_SYSTEM I.3 |
| D-9 | 界面语言 | **界面全部英文；既有测试数据不回改** | 所有用户可见文案、可访问名、`aria-label` 一律英文。种子脚本新建的演示数据也用英文，但**已经存在的合成/测试数据不回改**（2026-08-04 追加裁定）——它是测试夹具，不是界面文案，中文留在库里可接受。因此种子脚本不再检查、也不再报告残留的中文显示名：把已被判定为可接受的事标成警告，只会催人去「修」一个不是问题的问题。 代码注释与本仓库文档保持原样。**四份设计规范里的中文确认文案自此视为「意图」而非「原文」**——实现中的英文字符串是措辞的现行事实来源；规范未逐条回译，这是已知且有意接受的不一致。诚实措辞的约束不随语言改变（投递状态、受保护存在、草稿可见性、贡献≠证言等在英文里必须同样成立） | 用户指示，2026-08-04 |
| D-10 | 参与者底部栏放几个目的地 | **四个：Home / Consent / Messages / Help** | 英文标签比中文长，五个槽位在真机宽度下装不下。实测（Chromium，root 18px，`--type-size-0`）：单个槽位 = (视口 − 18px 外内距 − 槽位间距) ÷ n，标签实际需要 53–77px。五槽在 320px 只给 56.8px，而 `Community` 需要 77px——390px 截图里它折成了 `Com/muni/ty`；四槽在 320px 给 72px，最宽的 `Messages`（当前项加粗时 67px）仍是整词单行。**让位的是 Community**：它是唯一被研究本身定义为可选的目的地，Doc 20 §33 要求常驻的是 Consent 与 Help，不是 Community；它仍在首页任务清单里，且 Home 恒在栏内，故任意屏到社区最多两下。同时补齐两条：底部栏在 200%/400% 放大时**换行成多行而非横滚或裁字**（I.2 已否决横滚导航），`main` 的下内距由 `--nav-primary-height` 按实测高度写入，避免两行时遮挡确认区。320/360/390/430px × root {18,24,32}px 六种组合已验证：无裁切、无横向溢出、触控高度均 ≥44px | D-3 的实施约束 / DESIGN_SYSTEM §G 视口矩阵 |
| D-11 | 数据集定义批准人与锁定人是否须为不同人 | **可以相同** | 与后端现状一致：`lockDatasetVersion` 只校验版本处于 `Quality Reviewed` 与 `dataset.lock` 权限，不比对定义的 `approved_by_actor_id`，因此无需改后端。锁定屏**明说这条规则**（`Locking is not barred by separation of duties…`），而不是靠沉默让批准过定义的人自己猜是否被允许——沉默会诱使人去点一个不确定的按钮。同时仍在每一行显示定义的批准人（M12 lockable 查询新增 `definitionApprovedByActorId`），并在批准人就是当前身份时标注「that is you, which is permitted here」：**允许不等于不可见**，链条即使不设卡也要可读。注意与之相邻的另一条规则不同：定义的**起草人不能批准该定义**（`created_by_actor_id <> approver`，DB 层强制），这条仍然成立，本裁定不触及 | RESEARCHER U2 |
| D-12 | PublicProfile 缺位时显示什么 | **显示参与者姓名** | 参与者之间不再出现内部标识：社区 feed 的作者、会话对象、连接对象一律显示 `participants.display_name`（M18 三个查询经新增的批量端口 `findDisplayNames` 解析，避免 feed 上 100 次单查）。**这修掉了一个已记录的真实缺陷**——原来界面直接渲染内部主键，等于把跨屏关联参与者的把手交给每一个读者。解析不到姓名时显示统一占位 `A community member`，**绝不回退到 ID**；占位对所有人相同，因此不能用来区分他们。**须知的代价**：Doc 20 §354 禁止把 ParticipantProfile 与 PublicProfile 合并，而 PublicProfile 目前尚未实现（B14 未实现），所以这不是边缘情况——**当前每一个对外显示的姓名都来自研究侧档案**，参与者无法按 §132 逐项选择自己被如何称呼。社区页因此明说「你发布的内容会带上参与者记录上的姓名；目前还没有可另选的社区用名」。B14 实现后应改为读 PublicProfile，本条即失效 | PARTICIPANT C2 |
| D-13 | 管理工作区能否列出参与者账号 | **必须能列出** | 新增权限 `participant.list-administrative`，只给 OrganisationAdministrator，**须声明 `platform-administration` 目的**，且**不设同意门**——这条只返回标识、姓名、账号状态，不含任何研究内容；若按 `study-participation` 同意来门控，撤回同意的人会从他仍然需要的账号管理里消失。受保护存在（ADR-050）不因此削弱：危险的形态是「拿调用方给的标识回答它是否存在」，而这里不接受任何标识，只在管理员已持有的范围内枚举。**组织范围一律取自请求上下文，绝不作为参数或查询串**——集成测试抓到过一个真实漏洞：当它还是参数时，引擎优先用资源上的组织，于是范围受限于 A 组织的管理员在 B 组织上下文里传 A 就能通过；现在权限判定与取数用同一个值，且调用方无法选择它。**已知缺口**：参与者与组织之间没有直接关联，只能经「账号 → 组织成员关系」连接，因此**没有平台账号的参与者不出现在列表里**，界面明说这不是完整名册。另外发现账号创建时并不写入组织成员关系（全库仅 3 条），列表因此会恒为空——已让演示种子在建账号时带上组织。**平台运维角色（SystemAdministrator）不获得此权限**：运维软件与看见研究里的人是两件事 | RESEARCHER U3 |
| D-14 | AI 消息草稿助手本阶段是否启用 | **暂不启用** | 代码里当前不存在任何 AI 起草入口（已核查：apps/web 无相关组件，权限目录无相关动作键），因此本裁定不需要改动实现，只作为「不要去建」的记录。若将来启用，仍须先回答被搁置的那个问题：是否向接收方披露该消息由 AI 起草——不披露会污染被研究的干预本身（参与者以为在读一个人写的话），这不是一个可以事后补的产品细节 | PARTICIPANT C5 |
| D-15 | 协助模式的身份模型 | **只读协助；消息向接收方披露** | 协助者**永远不代参与者操作**：不引入第二身份，`ActorReference` 与审计表都不改——因为在只读协助下，记录里「谁做了什么」从头到尾只有参与者一个人，本来就是真的。新增的是**对「谁在场」的诚实**。参与者自己开启与关闭（能被别人打开的模式，是披着「帮助」外衣的监视），常驻横幅写明「X 正在帮你」「他们能看到这个屏幕，包括你的消息」「X 不能替你按任何东西」。**协助者姓名只留在本机、绝不外传**——横幅需要它才能说人话，别人对它没有用途。**消息披露**：协助期间确认发出的消息，接收方会看到「发送时有人在协助对方，那个人能看到这段对话」。披露的理由不是作者身份（消息确实是参与者自己按的），而是**这段对话的听众比另一方以为的要大**，这是对方有权知道的。落地为 `messages.sent_with_assistance`（迁移 1753800000020）——**只存事实，不存协助者是谁**：把一个第三方的身份塞进他并不参与的对话里，既不必要也不正当 | PARTICIPANT C7 |
| D-16 | 生命故事的可见性与受众选择（B11）本阶段是否上屏 | **不上屏，等有读者** | 与 D-5 同一条规矩：`life_story.items.visibility` 现在写得进去，却**没有任何读取路径会去看它**。全仓库唯一读取生命故事条目的查询是 `getMyLifeStory`，而它是 `ownerOnly` 的——也就是说把一条内容改成 `Community` 或 `Connections`，实际的受众依然是零。此时给出「分享给社区」的控件，参与者会以为自己已经分享了而其实没有；这个方向的谎言和「以为私密其实公开」同样是谎言，只是伤害形态不同。`Selected People` 更明确：授权表 `life_story.access_grants` 根本没有任何命令能写入（只有撤回时的批量 revoke），所以「选定的人」是一个没有办法选人的选项。**当前实现**：生命故事屏只**显示**每条内容的可见性，不提供更改入口，且措辞按当前事实说（默认只有你能看到）。**解锁条件**：先实现一个尊重 `visibility` 与 `access_grants` 的非本人读取路径，以及一个写入 `access_grants` 的命令；届时 B11 才有东西可以诚实地控制 | 实现推导，2026-08-04 |
| D-17 | 参与者自填评估（B15）本阶段是否上屏 | **不上屏，缺两样东西** | 缺的第一样是权限：`assessment.record` 是 `{}`，不带任何 owner 作用域，只给 ResearchCoordinator——**参与者根本不能填自己的评估**，命令也不校验 `enrolmentId` 属于谁。缺的第二样更根本：**平台里没有量表题库**。`assessment_records.instrument` 与 `instrument_version` 是自由文本，没有任何表存放题目、选项、计分或版本（`research_design.research_questions` 是研究问题，不是问卷题目）。要把评估屏做出来，实现者就得编出题目内容——**量表题目是经过批准的研究材料，编一份出来会让研究收到的是一份没人批准过的工具产生的数据**，这比界面缺一屏严重得多。**连带后果**：首页「等你决定的事」区块刻意不列评估，尽管 `Scheduled` 与 `Available` 的记录确实存在；列一件参与者做不了的事，等于指一扇他打不开的门。**解锁条件**：（1）经批准的量表内容与版本化题库落库；（2）新增 owner 作用域的 `assessment.record-own`，资源归属由 enrolment 上的参与者推导，不接受调用方传入（D-13 的教训） | 实现推导，2026-08-04 |
| D-18 | 「切换使用者」是否连共享设备标记一起清掉 | **保留标记，其余全清** | §D.6 把切换使用者写成「完全清空 `sessionStorage`」，而共享设备标记正存在那里——照字面做，最保护人的那个设置会在陌生人坐下的那一刻消失。平板并没有因为使用者换了人就不再是公用的。**落地**：`endVisit()` 清空 `sessionStorage` 后把标记写回；关于人的一切（偏好、其他会话数据）不写回。**同类偏离**：环境访问口令仍留在 `localStorage`。它是原型环境的门钥匙而不是关于人的信息，清掉它会让整台公用平板从此进不来，除非重新拿到当初的邀请链接 | 实现推导，2026-08-05 |
| D-19 | 会话超时警告用粘性还是对话框 | **对话框** | §D.5 一边把「会话超时警告」列进粘性元素，一边规定粘性元素总高 ≤ 视口 25%，两条量出来是矛盾的：320×844 下这段话（标题＋为什么＋会丢什么＋倒计时＋两个按钮）占 665px，即 79%；xxl 下 790px。压进 211px 的唯一办法是删掉「你打了还没送出的内容会丢失」，而那正是这个对话框存在的理由。25% 上限管的是与内容并存、常驻抢占空间的框架元件；打断式警告在那一刻本身就是内容。**落地**：固定定位、贴视口上沿、`max-block-size` 加内部滚动（居中会在字号放大时把按钮同时挤出上下两端）。**不加遮罩、不声明 `aria-modal`**：在它后面点一下就是「人还在」，计时器因此重置、警告随即消失——那正是它要问的问题的答案 | 实现推导，2026-08-05 |
| D-20 | 超时警告是否沿用设计稿「已保存成草稿」的措辞 | **不沿用** | §E.11 的警告文案写着「你写的内容已经保存成草稿」。本平台没有任何地方保存草稿——这句话会在它即将被打破的那一刻做出承诺。**落地**：改说真话「你打了还没送出或保存的内容会丢失」，并由测试守住「saved as a draft」不得出现。**连带**：§E.11 另有一条「超时不得静默作废进行中的同意变更或评估」，在没有草稿机制之前只能靠「先警告、再登出」来满足，不能靠「已经存好了」来满足；真正的落地要等草稿功能 | 实现推导，2026-08-05 |
| D-21 | 治理保留（governance hold）本阶段是否上屏 | **不上屏，它冻不住任何东西** | 与 D-5、D-16 同一条规矩。`governance-hold.place` / `.lift` 两个命令都在，`governance_audit.governance_holds` 表也在，**全仓库没有任何一处读它**——没有任何命令在改动某个制品之前去查它有没有被保留。也就是说今天放上一个保留，那份制品照样能被编辑、批准、导出。这里的谎言比「记录了没人读的决定」更危险一层：放下保留的人会**因此停止去找别的办法**拦住这件事。**解锁条件**：先让某条写入路径在改动前查询该制品上的活跃保留并据此拒绝（放在哪一层是架构决定：内核统一守卫，还是各模块自查），届时保留才有东西可以诚实地控制。**当前实现**：治理屏用一节明说这件事为什么不在这里，而不是留一个空白让复核人自己猜 | 实现推导，2026-08-05 |
| D-22 | 紧急访问（break-glass）如何措辞 | **叫「记录」，不叫「取得访问」** | 权限引擎从不查询 `break_glass_records`，所以执行 break-glass **不会给任何人任何权限**，记录里那个 `expiresAt` 也不会到点关掉任何东西。这与 C15「记录一次分析运行」是同一个形状：动作发生在平台之外（控制台、数据库），平台持有的是**对这件事的交代**。因此按钮叫「记录这件事」，屏上第一段就说「这不会给你访问权」，到期时间旁边说明「到那个时候不会有任何东西自动关闭」。**这里的措辞比别处更要紧**：把它写成一扇门，会让真的处在紧急情况里的人按下去，然后以为自己进去了。**保留的部分**：事后复核是真的——记录本身就是制品，复核在它上面写下「该不该发生」，而且**复核人不能是记录人**（命令强制），行内在按钮之前就说明。目前记录人没有回看自己那条记录结局的界面，屏上因此不承诺会通知 | 实现推导，2026-08-05 |
| D-23 | 审核决定：十个结果里哪些能上屏 | **四个真的 + 一个「记录你在别处做过的事」；五个不上屏** | 这是本仓库迄今为止最严重的一次「记录了没人读的决定」。`moderation_decisions` **没有任何一处读它**，而在本次修复之前 `post_state` 的 `Hidden` / `Removed` / `Restored` 三个值虽然写在 CHECK 约束里，**也从来没有任何代码写过它们**——也就是说「移除内容」把案件关成 Actioned、告诉审核员做完了，而那条帖子还好端端留在社区里。审核员、举报人、被举报人三方都会以为发生了什么事。**改法**：社区流本来就只显示 `Published`，所以补上写入端即可——`Hide` / `Remove` / `Restore` 现在在同一事务里改帖子状态，于是这三个是真的；`Dismiss` 关案件，关案件就是它的全部效果。**「警告」保留但改写**：它是「你在别处跟对方谈过，这里记下这件事」，与 C15「记录一次分析运行」、D-22「记录紧急访问」同形，屏上明说平台不会发送任何东西。**不上屏**：限制功能、暂停账号、断开连接、封禁——平台里根本没有任何账号限制机制，一个叫「暂停账号」的按钮会关掉案件、报告成功、然后那个人继续发帖。**升级也不上屏**：从审核到安全没有任何通路（E6 未实现），记录一次升级只会把案件移出你手里而没有移进任何人手里。屏上用一节点名这五个为什么不在，因为找不到「暂停」的审核员需要知道它是平台做不到，而不是这块屏藏起来了 | 实现推导，2026-08-05 |
| D-24 | 举报一条帖子时，被举报人由谁指定 | **由帖子指定，不由举报人指定** | 与 D-13 同一条教训：请求里点名的权限不是权限。举报人若能同时指定内容与「被举报人」，就能拿别人的帖子配上第三个人的标识，案件便按举报人的一面之词开在了错的人头上。**落地**：`submitUserReport` 在收到 `reportedContentId` 时从帖子的作者推导被举报人，忽略调用方传来的值。**连带好处**：社区流因此不必携带作者的标识——那正是 D-12 要挡在屏幕之外的那个可用于关联的把手。**同时补上主路径**（D-4 要求「从上下文一键发起」）：此前唯一的举报入口是帮助屏里手打对方标识，**没有任何路径能让一份举报指到一条帖子**，于是刚刚做成真的那三个内容决定根本没有东西可作用 | 实现推导，2026-08-05 |
| D-25 | 审批屏只有「同意」一个出口 | **补上「说不」，凡是 schema 里已有拒绝状态的都补** | 用「CHECK 约束里定义了、却从来没有任何代码写过」这条机械判据扫全库，扫出来最严重的一条：**全平台八个审批命令，没有一个能拒绝**。而其中五个制品的状态词表里**本来就写着拒绝**——协议版本、证据决定、分析计划、研究发现有 `Rejected`，证据评审有 `Returned for Revision`——没有任何代码路径写得进去。**一个只有一个出口的决策屏不是决策屏**：审批人清空队列的唯一办法就是把里面的东西全部批准，而屏上还写着「你可以决定这一版」。职责分离、精确版本、内容哈希、确认对话框，全都建在一个从来不存在的选择之上。**落地**：迁移 1753800000021 给这五张表加 `refused_by_actor_id` / `refused_reason` / `refused_at`，并加两条 CHECK——拒绝人不得是提交人（与批准同规则），以及**没有理由就不算拒绝**。命令层：拒绝与批准**共用同一个权限键**（它们是同一份权限的两种行使方式，与 C17 导出决策已有的做法一致），因此 `finding.approve` 是 MFA 级，拒绝一条研究发现同样要 MFA——拒绝不是决定里便宜的那一半。理由必填，因为工作被拒的人得能查到为什么；**拒绝一条证据决定不写快照**——快照是「已同意的决定依据了什么」的不可变记录，被拒的决定没有任何人可以事后引用的依据。**仍缺**：数据集定义、解释记录、报告版本三张表的状态词表里**没有**拒绝状态，补它们要改词表而不只是加列，留待单独裁定 | 实现推导，2026-08-05 |
| D-26 | 加入了却出不来：社区成员资格与连接的退出 | **两个出口都补上；退出不得依赖当初进来的那份权限** | 同一条机械判据（CHECK 里定义了、没有任何代码写过）扫出一对同形缺陷：`membership_state` 的 `'Ended'` 与 `connection_state` 的 `'Disconnected'` 从一开始就写在约束里，**没有任何代码路径写得进去**。也就是说社区可以加入不能退出——「参与社区完全是自愿的」只在第一次为真；连接可以建立不能解除——**唯一的出口是屏蔽**，而屏蔽是一个关于对方的安全动作，于是一次平常的道别只能被装扮成一次指控。这是平台加给人的压力，不该有。**两处执行端本来就在**（动态流与发帖都要求 Active 成员资格；消息命令在 basis 非 Active 时拒绝并抛 `COMMUNICATION_BASIS_EXPIRED`），缺的只是写入端。**关键裁定**：`community.leave` **不带任何同意前置**。加入受 `community-participation` 同意门控；若退出也照此门控，撤回那份同意的动作本身就会把人困在那份同意带他进去的社区里——**出口绝不能依赖当初让你进来的那份权限**。**连接一方即可解除**：要求双方同意，等于想走的那个人得等另一个人点头，那就不叫出口了。**平台不通知对方**——一条「X 解除了与你的连接」是在宣告某人退出的决定，而能安静地退出正是它安全的一部分。解除时同事务把该连接上的会话置为 `'Expired'`（这个状态的文案「这段对话之所以可能的那个原因已经结束」早就写好且一直够不到）：留成 Active 会让列表写着「进行中」，而每一次发送都会失败。**两处都不是删除**，屏上明说 | 实现推导，2026-08-06 |
| D-27 | 确认为安全事件之后呢 | **补上事件视图、动作记录与状态推进；措辞不得让「结案」读成「人安全了」** | `event_state` 默认 `'Open'`，**没有任何代码路径改得动它**，没有任何查询列出安全事件，也没有任何屏幕显示过一个。分诊屏告诉安全审阅人「已转为安全事件」——这句话读起来像升级给了某个会去处理它的东西——而**没有任何东西去处理它**。这是本平台持有的最严重的一条记录（一个人确认了另一个人可能有风险），它进了一张没人能读、没人能动的表。读取权限 `safety-event.review` 一直就在权限目录里，只是没有东西可读。**落地**：迁移 1753800000022 加一张**只可追加**的 `safety_event_timeline`（触发器强制），动作与状态推进写在同一条时间线上——不然「发生了什么」会被劈成两半各说一半；更正是追加一条，原记录留着（与审核决定同规矩：一条事后可以整理干净的安全记录，就不是记录）。状态机在命令里，界面只提供当前状态允许的那几步（否则又是一个按不动的控件）。**三条措辞是这次的实质**：（1）屏上第一段就说**平台自己不做任何事**——不联系任何人、不限制任何东西、不监测任何东西，这里的每一条都是某个人写下他做了什么；（2）选到「已解决／已结案」时明说**这不代表那个人安全了**，这是本平台可能出现的最危险的一句话；（3）记录框旁边说**写做了什么，不写说了什么**——通话内容属于临床记录，不属于这里。**空状态也是判断**：没有任何动作时屏上说「空白不等于已判断」，因为后来读的人分不清「不需要动作」和「没人看过」，所以「决定不需要采取动作」是一个真选项。**诚实的缺口**：设计要求显示「与干预的关联性」，平台任何地方都没有记录它——屏上明说「不是没人判断，而是平台没有地方放这个判断」，而不是默默不显示 | 实现推导，2026-08-06 |
| D-28 | 支持者关系在产品里没有入口 | **协调员屏补上「提议一位支持者」；可授权的动作只列平台真的会读的那两个** | `POST /v1/relationships` **全产品没有任何调用方**——整条支持者路径（家人看到参与者选择分享的内容、为生命故事提供补充、参与者批准或拒绝）只有能直接调 API 的人才能开启；演示环境里它存在，仅仅因为种子造了它。协调员没有办法把一位家人带进任何一个参与者的研究。**两处措辞是实质**：（1）**提议不授予任何东西**——由参与者本人批准，在他批准之前被点名的那个人什么都做不了；协调员若误解这一点，会去告诉家人「你已经有权限了」。（2）可授权动作**只列 `participant.view-shared` 与 `life-story.contribute`**，因为全平台**只有这两个动作是由关系门控的**（`requiresRelationship`），其余一切要么按角色、要么按「是你自己的东西」判定，根本不会去看这张清单——列长了就是记录一份没人读的权限：支持者被告知他可以做一件他做不了的事，或参与者以为自己限制了其实没限制的东西。屏上点名这条限制，免得清单短得像是被藏了东西。每一项还写明它另外依赖哪份同意（没有那份同意时它什么都不许可） | 实现推导，2026-08-06 |
| D-29 | 参与者能给陌生人写信，却不能给自己授权的家人写信 | **裁定：做成关系上一条独立的可授权动作；已实施（2026-08-06）** | `conversation_threads.basis_type` 的 `'AuthorisedRelationship'` 从来没有被写过：`createThread` 只接受 connection 并把 basis 写死。也就是说**平台让你和算法推荐的陌生人通信，却不让你和你自己批准的支持者通信**，而消息屏里「你批准了这个人作为支持者」的文案一直摆着、永远不可能出现。对一个关于长者与社会连接的研究，这个方向是反的。**裁定的那一条**：「可以给我写信」是关系上**一条独立的可授权动作** `relationship.message`，不是 `participant.view-shared` 的顺带结果——一位参与者想让侄女读自己的生命故事、仅此而已，否则就等于顺手给了她一条通往自己收件箱的通道。**三个解锁条件全部落地**：（1）支持者侧收件箱 `listThreadsForActor` **按 actor 而不是按 participant** 查——正因为缺它，此前不能做：只能被写入、读不到的通道比没有通道更坏，因为写的人以为自己被听见了；（2）`relationship.message` 用 `requiresRelationship` 而**不用 `ownerOnly`**——关系的 `permitted_actions` 真的决定他能不能写；（3）**ADR-031 的基础复评按 basis 分支**：参与者撤回关系（或把它收窄），会话在那一刻就停，而不是等下次有人想起来检查。**另外两条**：会话由参与者开启，支持者不能主动开；`relationship.message` 故意**不挂 `supporter-involvement` 同意**——那份同意管的是支持者能看见什么，写信是另一回事，由关系本身的授权决定。发送仍然要确认（守卫写在命令里，因为把动作设成 confirmationRequired 会连打草稿都要求确认） | 实现推导，2026-08-06 |
| D-30 | 参与者自己批准的支持者，在会话列表里显示成「A community member」 | **不猜；模块说「我不知道」，名字在组合根解析；已实施（2026-08-06）** | 关系会话的另一侧是**账号**（M01 user account），不是 participant，所以拿它去查参与者名册**不会报错，只会查不到**——而查不到会落到那句为「社区里认不出来的陌生人」写的占位文案。这里错了两次：支持者不是社区成员；而且这句占位**按设计对所有人完全相同**，于是一位批准了两位支持者的参与者会看到两行分不清谁是谁的会话——偏偏这是「知道对面是谁」最要紧的一类会话。**做法**：`listThreads` 对 `AuthorisedRelationship` 的行不再去查参与者名册，返回 `null`；名字由控制器用 `accountNames`（M01）解析，与关系列表一直以来的做法一致——账号名归 M01，组合根才是两者允许相遇的地方。`listThreadsForActor`（支持者侧）同理去掉占位：支持者与他支持的人并不在同一个社区里。**界面**：解析不到时写「Someone whose name is missing from this list」——更难看，但可信：看到的人知道是记录缺了，而不是以为自己被一个陌生人写了信 | 实现推导，2026-08-06 |
| D-31 | 审计访问的「访问理由」是否必填（D-6 的重新评估） | **改为必填；后端现在真的记录它；已实施（2026-08-06）** | D-6 当时裁定不必填，理由写得很清楚：后端不记录这个字段，**前端必填、后端不记录**是「假的保障」。那条裁定同时写明了自己的解除条件——「若将来后端记录该字段，再评估必填」。本次实施把条件补齐了：迁移 `1753800000023` 给 `audit_events` 加了 `access_reason` 与 `query_filters` 两列（可空，61 处命令写入一律留空，只有读审计本身会写），查询把理由和筛选条件**在返回任何一行之前**写进同一张只追加表。写不进去就读不到——与命令同一条 fail-closed 规矩（ADR-051），而且在这里比在别处更要紧：没有被记录的那一次翻阅，正是有人想要的那一次。既然它被记录了，必填就不再是空头承诺，`RESEARCHER_WORKSPACE §21 G7` 的原始设计（必填以提高滥用成本）恢复。屏上写明理由会被谁看到——把字段当摆设的人会往里写摆设 | 重新评估 D-6，2026-08-06 |
| D-32 | 审计记录里没有「谁看过」，也没有「谁被拒绝」 | **照实说；不在界面上假装有；已实施（2026-08-06）** | 读取路径实现后逐条核对了这张表实际存了什么：**61 处写入全部来自命令**，没有任何一个查询写审计——所以「谁看过我的档案」这个问题，平台答不了。`audit_events.result` 的 CHECK 里有 `'Allowed'` 和 `'Denied'`，而**全代码库没有任何一处写过这两个值**（只有 `'Succeeded'`／`'Failed'`）：权限引擎拒绝一次，什么都不留下。于是有人来查一次未遂的滥用，会一无所获——而那个「无」只说明这张表从来没被告知过。G7 屏把这两句放在表格之前、任何人读到第一行之前说清楚，空结果处再说一次「这不等于没有发生」。**没有做的**：不给参与者做「谁访问过我的数据」——那需要读取路径先写审计，在此之前做出来就是一块永远显示「无人访问」的屏。**解除条件**：（1）权限引擎在判定时写 `Allowed`／`Denied`；（2）读取路径按数据分级记录访问。两条都落地后，G7 的措辞与参与者侧的可行性都要重评 | 实现推导，2026-08-06 |
| D-33 | 同意文本改了，平台没有任何办法说这件事 | **补上写入方；由批准人记录，必须写明改了什么；已实施（2026-08-06）** | `consent_decisions.decision` 的 CHECK 里从建表起就有 `'ReConsentRequired'`，**权限引擎读它**（`engine.ts` 第 5 步：该范围下的一切当场拒绝），`assert.ts` 也为这个 outcome 备好了文案——一整套「你同意过的条款变了」的机制建好了、接通了、**没有任何一处能触发它**。于是同意文本可以改版，而每一位参与者继续挂在一份对已不存在的措辞的同意上，自己的同意屏还对他说「已同意」。**做法**：`consent.require-reconsent` 授给 ResearchApprover（改动条款的那一版正是他批的），`confirmationRequired`，**故意不是 ownerPermitted**——这是别人对你的同意做的唯一一件事，而且方向是收回而不是给予。**三条拒绝比那次写入更要紧**：（1）没有可被取代的同意（该范围是拒绝／撤回／未决）就拒绝——否则按下去什么都没变，却告诉按的人他做了事；（2）版本号与参与者已同意的相同就拒绝——那只会切断他的访问、又不给他任何新东西可读；（3）**必须写明改了什么**（迁移 `1753800000024` 加 `decision_note`）——不说改了什么就要人重新同意，那不是请求而是阻断：他无从判断被要求判断的东西，实际只剩「同意」一条路。参与者屏上用现在时说「已经停了，不是将要停」，并写明拒绝不牵连其他选择与他在研究中的位置 | 实现推导，2026-08-06 |
| D-34 | 平台最核心的那个模块（干预组合 M06）在产品里完全看不见 | **补上查询与两侧屏幕；证据分级照实说「没有」，不显示假分级；已实施（2026-08-06）** | M06 有六条路由——建干预、加版本、提交、批准、启用、按项目配置——**产品里没有任何一处调用它们**；而且 M06 **一个查询都没有**，所以哪怕直接打 API 建出来的干预，从那一刻起也再没人能看见。于是：批准人手上有两个决定，却无从得知有东西在等；提交了版本的研究者，查不到它后来怎么样了。对一个以「长者数字干预」为题的平台，干预恰好是唯一看不见的东西。**做法**：新增 `intervention.view`（研究者与批准人同持——他们是这次交换的两端，扣着列表等于让那两个决定无法抵达），列表按干预带出版本；研究者屏可建干预、加版本、提交，并**明说批准与启用不在这里**（提交人不能是批准人，服务端与数据库都拦）；批准人屏由列表驱动，每行只给它当前状态接受的那一个控件，启用前的确认写明**它顶掉哪一版**（唯一索引保证同一时刻只有一版在用，「启用」这个词把这层意思藏起来了）。**证据分级刻意不显示**：`evidence_status`（E0–E5）与 `evidence_direction` 两列**没有任何代码写过**，全库每一行都是默认值；把「E0 / Not Evaluated」印在干预旁边，会被读成有人评过它并给了最低档——在一个讲证据的平台上这是最误导的一种空白。屏上写明这两件事「平台里没有任何地方能记录，所以没有东西可显示；不是评过之后结果低」。**解除条件**：等证据评审的结论真的能写回干预（M10→M06 那条线），再把分级摆上去 | 实现推导，2026-08-06 |
| D-35 | 「已生成」的导出包里没有数据；参与者却被告知「你的副本已经做好了」 | **按事实改写措辞；平台产出的是清单不是数据；已实施（2026-08-06）** | `generateExportPackage` 写的是一份 **manifest**：这次导出自己的类型、来源、去标识与限制，外加对该 JSON 的 SHA-256。**它不读任何参与者数据、不汇集任何记录、不产生任何文件。** 而两块屏都把它说成「把包做好了」：工作人员侧写着「整理成包这件事是平台做的」，参与者侧写着「副本已经做好，尚未交付」，`Delivered` 更写着「副本已经发送给你」。**对参与者而言这是本轮发现里最重的一条**——一个人行使自己的数据权利，被告知副本已存在、已寄出，于是去等一个平台从来不会产生的文件、去查一个这里什么都不发的收件箱。**做法**：措辞按事实——平台记录的是「经同意可以外发的确切范围」，汇集数据、生成文件、送到人手上都是人在平台之外做的事；哈希的全部价值正在于此（拿组装出来的东西对照当初批准的清单），一旦被读成「平台决定了文件里装什么」，这个价值就没了。参与者侧还改了 `Delivered`：那是某个人记下自己做了什么，**不是送达的证据**——没收到就说出来。**没有做**：真正汇集参与者数据（要跨模块读取与去标识裁定，且那是一次实现而不是一次措辞修正）。**解除条件**：等平台真的能组装内容，再把「副本已做好」这类说法放回去 | 实现推导，2026-08-06 |
| D-36 | 聊天消息记录了「有人在旁协助」，同意却没有 | **同意也记录协助事实（只记事实、不记是谁）；已实施（2026-08-06）** | `consent_decisions.assistance_recorded` 从第一版同意迁移起就在，**没有任何代码写过它**；与此同时平台有协助模式，协助状态下发出的消息带 `sent_with_assistance` 并向收件人明示。也就是说：平台记录了某人是在旁人帮助下写出「周四我把幼苗带过来」，却不记录同意参加研究时身边有没有人。对一个以长者为对象的研究，这个方向是反的——「当时有没有人在场」正是伦理复核、日后关于能力的疑问、乃至参与者自己会需要的那个事实，而且事后无法重建。**做法**：记录与撤回同意都写入该事实；投影表加一列，参与者自己的同意屏能就「当前这条选择」显示「在他人协助下作出」；屏顶在协助模式下先说清楚会记录这件事——**一件被记录却没被告知的事实不是保障**。**只记事实、不记是谁**：协助者的名字由参与者自己输入、从不离开设备（D-15），记下名字等于把人家的家庭情况写进研究档案；测试直接断言 `consent_decisions` 没有任何 helper／assistant 列。措辞明说这不会让同意「打折扣」 | 实现推导，2026-08-06 |
| D-37 | 前端把同意文本版本写死成 `ct_v1` | **改为提交「参与者当时看到的那一版」；已实施（2026-08-06）** | API 客户端里 `templateVersion: 'ct_v1'` 是硬编码。在没有任何东西能改动同意文本的年代它无害；**D-33 落地那一刻它就错了**——被要求对新措辞重新同意的人，会被记录成「同意了旧版」，而那恰恰是这次要求存在的意义所在。现在提交的是屏上正在显示的那一版（`state.templateVersion`）。平台仍**没有同意模板注册表**，所以「尚未决定过」的范围仍回落到 `ct_v1`；这一点写在代码注释里而不是假装有 | 实现推导（D-33 的后续缺陷），2026-08-06 |
| D-38 | 权限只能给出去，收不回来 | **补上「谁持有什么」列表与收回；已实施（2026-08-06）** | `revokeRole` 自 M01 写下之日起就齐备——权限校验、乐观版本守卫、领域事件、审计条目——**没有路由，也没有任何屏幕**；`user.view` 授给了组织管理员与协调员，**没有任何代码检查过**；没有任何查询列出过账号或角色。也就是说：一位离开研究的协调员、一位参与结束的研究者、一个本该关掉的账号，**在产品里都动不了**，挡在旧同事与参与者记录之间的只是「没人想起来查」。**做法**：按请求上下文里的组织列出账号及其角色（组织**不来自参数**——接受组织标识的列表就是一种「哪些组织存在」的探针），收回需确认且绑定版本（记录在他人手里变过就拒绝，不合并）。**两处不许夸大**：（1）**收回角色不等于关闭账号**——`account_state` 有五个取值、没有任何代码写过任何一个，`Active` 是列默认值而不是谁的判断；把它当状态显示，等于回答一个平台从没被问过的问题，所以屏上明说「这里关不掉账号，删光角色是唯一能停下一个人的办法」，并说明账号仍可登录、只是每个动作都会被拒（平台也拦不住登录本身）。（2）**收回的是「之后能做什么」，不是「已经做过什么」**——写过的记录还在，决定上仍署他们的名，审计留着他们的动作，那正是审计的用途；把「收回」读成「抹掉」的人需要当场知道这一点。测试断言收回后下一次判定是 `DenyAndHideExistence`／`no-granting-role`——不是「拒绝」而是**连存在都不透露**（ADR-050），因为让旧同事得知「这位参与者存在」恰恰是收回要防的事 | 实现推导，2026-08-06 |
| D-39 | `participant.view-shared` 没有任何代码读，却是「引入支持者」屏上默认勾选的那一项 | **照实标注为「不起作用」，取消默认勾选，三处措辞全改；已实施（2026-08-06）** | 全代码库**没有任何一次 `checkPermission` 用过这个动作**：没有查询以它为门，没有屏幕会显示支持者能看到的东西，参与者也**没有任何办法把某条内容标为「已分享」**（可见性控件属 B9，未实现；`life_story.access_grants` 表从建表起没被写过一行）。而它出现在三处：协调员的「引入支持者」屏上**默认勾选**、参与者的「谁能访问我」里写着「看到你同意分享给支持者的部分」、支持者自己的工作区里写着「查看他们选择分享的内容」。于是最短的一条操作路径就是：记录一项并不存在的访问权，然后请参与者批准它——**参与者会以为自己把生命故事分享出去了**，也会以为撤回它能收回什么；支持者则会去找一块不存在的屏，并合理地以为对方什么都没分享给自己。**做法**：不再默认勾选（默认全空）；条目本身标注 `NOT IN USE` 并写明「勾选只是记录一个意图，不授予任何访问」；若所选**全部**是这类空权限，在做选择的当场提示「你选的每一项平台都不会执行」；参与者侧与支持者侧改成「记录为……，但平台不会据此做任何事」。**不撤掉这个选项**：关系记录里本就带着它，意图值得留存——但任何一处都不许把它说成访问权。**解除条件**：先做出参与者可用的可见性控件（含 Selected People 的授权写入），再做支持者侧「他们分享给你的内容」查询，两者齐备后措辞才可以改回 | 实现推导，2026-08-06 |
| D-40 | 研究项目建完就再也看不见；研究问题的表从没被写过一行 | **补上项目列表与研究问题；生命周期不显示（因为没人写）；已实施（2026-08-06）** | `project.view` 授给研究者、**没有任何代码检查过**，也没有任何查询列出过项目：建一个项目，标识符在那条提示里出现一次，然后再也找不到——而下游每一块屏（协议版本、入组、数据集、导出）都要求把这个标识符**凭记忆敲回去**。研究链条的源头是唯一没有列表的一环。`research_questions` 自 M04 写下之日起就有表、有文本列、有状态列，**没有任何一行被插入过**，`question.create` 授出而无人检查；于是一个研究项目只有一个标题，「这项研究要回答什么」无处安放，而清单里 C2 的标题写着「研究项目与研究问题」。**做法**：按请求上下文里的组织列出项目及其问题（组织不取自参数，同 D-38 的理由），可记录研究问题。**两处照实说**：（1）**不显示项目的状态与阶段**——`project_state`／`project_phase` 合计 17 个取值、没有任何代码写过，全库都是默认值，印出来会被读成有人设过；（2）**研究问题不是控件**——平台里没有任何东西会读它，而这**不是缺陷**：研究问题的读者是人。这一条必须写明，否则它看起来就像本项目一直拒绝制造的那种「记录了没人读的决定」的控件；两者的区别在于前者假装有保护，后者本来就是文档 | 实现推导，2026-08-06 |
| D-41 | 干预配置（§67）刻意不上屏 | **原裁定「不做」；同日**因解除条件达成而上屏（见 D-42） | `intervention-configurations` 有命令、有路由、**没有任何调用方**；而且 `configuration_state` 的六个取值里，除了插入时的 `Draft` **没有任何代码写过任何一个**，也没有任何代码读过配置本身。也就是说：把它做成界面，等于新增一个「记录了决定而没有任何东西会读」的控件——正是 D-2／D-5 立下的规矩所禁止的，也是治理保留（D-21）当初不上屏的同一条理由。**解除条件**：等交付或会话记录真的会去查配置（哪一版干预按哪一版协议投给这个项目），届时再上屏 | 实现推导，2026-08-06 |
| D-42 | 干预可以被批准、启用，却没有任何地方能记录「有人真的收到了」 | **补上交付记录；同时解锁 D-41 的配置屏；已实施（2026-08-06）** | M07 整个模块只有**一条命令**，没有查询、没有路由、没有屏幕。于是干预可以起草、批准、投入使用，而**没有任何人能记录某位参与者收到过它**，也读不回已经记下的东西。对一个以「投放数字干预」为主题的平台，交付是唯一没有入口的一环。**顺带解锁 D-41**：会话记录必须指名它所对应的**那一份配置**（否则说不清参与者到底被暴露于什么），这正是 D-41 给自己写的解除条件——所以配置屏现在存在，理由与它当初不存在时是同一条。**措辞的要害（Doc 2）**：记录的是**实际**暴露而不是计划暴露，**交付不等于有效**。「Completed」只表示这一节走完了，不表示它起作用了；把两者混为一谈，会让一份交付日志变成结论声明，而研究报告会据此写出来。九个暴露状态每一个都配一句说明（「Declined 是他们的决定，记在他们名下」「Failed 是我们这边出了问题，不是他们」）。**不显示 `session_state`**：它默认 `Completed` 且**没有任何代码写过**，于是种子里那条「只收到一部分」的记录同时声称「已完成」——两者印在同一行会自相矛盾（测试直接断言这条数据事实） | 实现推导，2026-08-06 |
| D-43 | 分析运行的结果被写死成「Completed」，失败无法记录 | **由记录的人说明实际结果；失败不推进数据集状态；已实施（2026-08-06）** | `runAnalysis` 的 INSERT 把 `run_state` **硬编码为 `'Completed'`**。平台本来就不执行分析（人在别处跑，回来记录，这一点屏上一直写得很清楚），于是**每一条运行记录都声称干净完成**，无论实际发生了什么：跑崩了的分析只能被写成一切顺利，而据此写出的解释（interpretation）不带任何提示。**做法**：记录者从三种事后可以诚实说出的结果里选——完成／完成但有警告／失败；并且**失败的运行不再把数据集版本推进为 `Analysed`**（什么都没分析成，标成已分析会向下游所有人误述这份数据）；领域事件也随之区分。**`Queued`／`Running`／`Cancelled` 继续保持不可达**并在屏上写明理由：这里没有任何东西排队、执行或取消，记录成那三种状态等于描述一台并不存在的机器。运行结果同时出现在**选择要解释哪次运行**的下拉里——此前每条运行读起来都一样，选哪个都看不出它是否成功 | 实现推导，2026-08-06 |
| D-44 | 匹配与消息的日常拒绝在参与者侧一个字都没有 | **逐条补上措辞；不借他人之事解释拒绝；已实施（2026-08-06）** | 把模块实际抛出的错误码与 `apps/web/src/errors.ts` 的措辞表对照，36 个码里 28 个没有措辞。其中一大批**恰好是参与者日常会遇到的正常拒绝**：推荐过期、双方接受已被用掉、连接已结束导致会话不能再写、草稿在另一个窗口变了。它们全都落到兜底文案——「我们无法判断原因，也不知道是否已经生效」。兜底本身是诚实的（不知道就说不知道），但**用在平台明明知道发生了什么的地方就成了另一种不实**：把一件正常且可解释的事说成疑似故障，还请人再试一次注定同样被拒的操作。**做法**：15 个码逐条给出「发生了什么／什么保住了／什么没有发生／下一步」四件事。**一条硬规矩**：不得用「对方屏蔽了你／对方撤回了」来解释拒绝——`MUTUAL_ACCEPTANCE_INVALIDATED` 可能是撤回也可能是屏蔽，那是对方的事，不归我们披露；测试直接断言这些词不出现。**过程中改正了自己写错的一条**：`MATCHING_NOT_ACTIVE` 一开始被写成「去『我的同意选择』里打开」，但它检查的是 `match_preferences` 是否 Active（在「认识新朋友」里打开），同意范围管的是「有没有资格打开」、缺失时会以另一个码更早拒绝 | 实现推导，2026-08-06 |
| D-45 | 部署冒烟检查会因冷启动而误报「访问令牌闸门未生效」 | **只对「说不准」的回答重试；闸门放行必须立即失败；已实施（2026-08-06）** | `42baef7` 的部署失败，报 `access-token gate is NOT enforcing (expected 401, got 500)`。查日志确认：密钥已挂载（Deploy 步打印了 `HADI_ACCESS_TOKEN found`），修订版已接 100% 流量，`/health` 与前端外壳都是 200，迁移干净，而**那次提交只改了前端 `errors.ts` 与文档**——闸门代码与上一版逐字节相同。重跑即通过：500 来自一个还在启动的实例（`--min-instances 0`），此时中间件还不存在，答不出 401。**真正的问题不是那一次失败，而是这条检查没有重试**：上面的存活检查重试 6 次，闸门检查一次定生死。**一个因为别的原因而失败的检查比没有检查更坏**——它教会看板的人直接按「重跑」，而这正是将来某天真的「闸门放行」被一路放过的方式。**做法**：只对 `000` 与 `5xx` 这类「说不准」的回答重试；**放行的闸门会答 200／403／404**——稳定且非 5xx——必须当场失败，不给它六次机会看起来像偶发；真的坏掉则一直答 500，重试用尽后照样失败。四种情形都用桩脚本验证过（含「冷启动后转 401 必须通过」与「答 200 必须立刻失败」）| 实现推导，2026-08-06 |
| D-46 | 任何登录者都能伪造一条「AI 提出的」安全信号 | **来源声明在命令里强制；同时写明「不设权限检查」是有意为之；已实施（2026-08-06）** | HTTP 边界上写着注释「HTTP 调用方只能提出人类来源的信号；AI／Rule／Integration 来自平台内部（ADR-039）」，并配了一个 TypeScript 联合类型——**两者在运行时都会被擦除，而这个 API 没有任何请求体校验**（无 ValidationPipe、无 DTO）。于是任何已认证的调用方都能提交 `sourceType: 'AI'`：它会被原样写入、以 `AISafetySignalRaised`／`sourceModule: 'M11'` 发到 outbox、并在分诊屏上显示为 `source: AI`。**这个标签不是装饰**：ADR-039 的整套规矩就架在「哪些信号来自机器」之上（自动化可以提出、永远不能确认事件），而安全审阅者对机器的标记与对人的陈述权重不同。更要紧的是——**平台今天根本没有任何地方会提出 AI 信号**（M11 一条命令都没有），所以这类记录**只可能是伪造出来的**。**做法**：规则改在命令里执行（每个调用方都经过那里）：机器来源必须是 service-account 主体，人类来源必须是人；反向同样拒绝——机器冒充人比人冒充机器更糟。**同时把另一件事写清楚**：`recordSafetySignal` **完全不做权限检查，而且必须保持这样**——有人要为别人的安全出声时，不会因为没有角色而被拒之门外；`safety-signal.record` 挂在 SafetyReviewer 名下却无人检查，看着像疏漏，其实不是。测试同时钉住这两面：伪造被拒，以及**一个没有任何角色的人仍然能提出关切** | 实现推导，2026-08-06 |
| D-47 | 生命故事写下去就再也改不了，而屏上三处都在讲「改了会怎样」 | **补上修改入口；改动不覆盖旧版，且明说会解除证言确认；已实施（2026-08-06）** | `reviseItem` 有命令、有路由、**没有任何调用方**：参与者可以写一条，然后在产品里一个字都改不了。而这块屏**三处都在描述修改**——「这条写过 N 次，之前写的没有被覆盖，旧版都留着」、确认对话框里的「之后如果你改了文字，新文字要再确认一次才算」、以及直接显示 `supersedesConfirmedVersion`（「确认之后你改过它」——**这个状态只有改过才可能出现**）。**对一份生命故事来说，不能修正自己对自己一生的记述，是件很奇怪的强制**。**做法**：编辑框预填现有文字（是修改而不是从头再写一遍），保存写入新版本、旧版本全部保留；已撤回的条目不提供（命令本来就拒绝，摆一个按不动的控件与摆一个没用的控件是同一个缺陷）。**措辞在按下之前说**：如果这条已经被确认为「我的原话」，改动会让新文字回到未确认——确认属于当时确认的那些字，不属于这个条目；之前确认过的内容仍以原样留在记录里。来源仍写 `ParticipantAuthored`——这块屏没有起草助手（D-14），写成别的就是伪造来源 | 实现推导，2026-08-06 |
| D-48 | 「已撤回」的文案一直摆在屏上，而没有人能撤回 | **补上撤回入口；说清它不是删除；已实施（2026-08-06）** | `withdrawItem` 有命令、有路由、**没有任何调用方**，而这块屏早就写着「你撤回了这条。它现在是私密的，别人无法访问；它仍然为你保留」——一个谁都到不了的状态，用现在时描述着。**措辞的要害**：「撤回」在很多人读来就是「删除」。真正发生的是：条目转为私密、已有的分享授权当场全部撤销、不能再修改；**但内容不删**，作者本人仍能读，每一个版本都留着。想让它彻底消失的人，必须在按下之前就知道这一点。另外说明**撤回不会取消证言确认**：你曾经确认过那些是自己的话，这条记录照原样留着——撤回不是收回那句话。已撤回的条目不再提供修改与再次撤回（命令本来就拒绝）。**可见性控件仍然不做**：D-39 已经查明「分享给支持者」在平台上根本不存在（没有任何代码读 `participant.view-shared`、`access_grants` 从没被写过一行），做一个可见性选择器等于新增一个「记录了决定而没人读」的控件 | 实现推导，2026-08-06 |
| D-49 | 评估记录（M08）刻意不上屏 | **不做；平台没有量表题库，做出来只会请人现编测量字段** | M08 与 M07 一样只有一条命令、没有查询、没有路由、没有屏幕。但它与交付不同：`recordAssessment` 要求 `instrument` 与 `instrumentVersion`，而**平台里没有任何地方定义过一份量表**——`instrument` 是一个自由文本列，没有约束、没有题库、没有版本登记。做一块屏，就是请协调员现场编造量表名称与版本，再把分数填进去；一条署着编造出来的量表名的评估记录，比没有记录更坏，因为它读起来像是测过。D-17 当初据此裁定参与者侧不做（题目是经批准的研究材料，不能由实现者发明），同一条理由对工作人员侧同样成立。**解除条件**：先有量表登记（题目、版本、评分规则皆为经批准的研究材料），再谈上屏 | 实现推导，2026-08-06 |
| D-50 | 文件上传（`objects/*`）有完整管道、没有任何界面 | **可以做，但屏上不得说「已查毒」；目前不做** | 与前几条不同，这条**不是空壳**：`storage-pipeline` 的隔离流程是真的——上传完成即置 `Quarantined` 并写入 `checksum_sha256`，扫描后按结果转 `Available`／`Rejected`。四条路由没有任何调用方，所以参与者无法上传任何东西（生命故事只有文字）。**若要做，措辞有一条硬约束**：这里的「扫描」识别的是 **EICAR 测试串**（以及一个触发「扫描失败」的标记），它是模拟器而不是杀毒引擎；屏上写「已查毒」会是一句平台兑现不了的安全承诺。**第二条理由（同日核查后补记，比措辞约束更硬）**：**上传完还看不回来**。`assignObject` 把归属写在**对象那一侧**（`owning_resource_type` / `owning_resource_id`），而生命故事的表里没有任何指回对象的引用，也**没有任何查询能按归属资源列出对象**（只有按 objectId 查单个）。也就是说：参与者上传一张照片、挂到某条生命故事上，那条生命故事永远显示不出它——除非他自己记住了对象标识符。只做上传界面，等于让人把照片交给一个再也拿不回来的地方，而他会以为照片成了自己故事的一部分。**因此这不是一个增量**：要做就得连读取路径一起做（按归属资源列出对象 + 在生命故事里呈现）。**顺带记一笔自查**：我先前的「死列」扫描把 `checksum_sha256` 报成了从未被引用——那是扫描脚本自身的缺陷（提取标识符的正则 `[a-z_]` 不含数字，`checksum_sha256` 被截成 `checksum_sha`）。修正后 40 条降为 31 条；已落地的那些论断都是关于「有没有写入方」并各自单独 grep 验证过，未受影响 | 实现推导，2026-08-06 |
| D-51 | 措辞表里为一个平台根本没有的错误码备好了句子，而真正会发生的那个没有 | **改正拼写；补上工作人员真正会遇到的拒绝；用测试钉住「只为内核声明过的码写措辞」；已实施（2026-08-06）** | 参与者侧与工作人员侧**两张措辞表都为 `VALIDATION_FAILED` 备了句子**，而内核的码是 `VALIDATION_ERROR`——前者在全平台不存在、永远不会到达；后者恰恰是本轮新增的四道守卫（重新同意的说明、研究问题文本、审计访问理由、安全信号来源）会抛的那个，于是它们全都落到「我们无法判断原因」。**这正是本轮一直在产品里找的那类缺陷，出现在解释拒绝的那张表自己身上。** 同时补上工作人员实际会遇到而无措辞的拒绝：`APPROVAL_REQUIRED`、`DATASET_LOCK_NOT_READY`、`DEIDENTIFICATION_REQUIRED`、`RESOURCE_STATE_BLOCKED`、`ORGANISATION_CONTEXT_REQUIRED`、`CONFIRMATION_REQUIRED`、`INTERNAL_ERROR`（每条都对着抛出点核对过措辞，D-44 的教训）。**测试的判准刻意是「内核声明过」而不是「当前会抛」**：`CONSENT_REQUIRED` 是真实的码，但权限引擎今天不抛它——缺同意时返回的是普通拒绝，因为指名是哪道闸拦住了你，就等于确认那条记录存在（ADR-050）；若判准写成「当前会抛」，就会反过来主张删掉它。测试从内核源码读取码表、从 `errors.ts` 源码读取两张表的键，都不靠复述——复述进测试里的清单，会和代码一起漂移，正如这张表当初漂走的那样 | 实现推导，2026-08-06 |
| D-52 | 出站事件箱把每一条领域事件都标成「已发布」，而没有任何消费者存在；退出研究因此不曾传播到任何地方 | **不去补消费者；把退出改成在写入时刻生效（M07／M08），并把屏上那句「会传播到相关记录」删掉；已实施（2026-08-06）** | `EventDispatcher` 在 `apps/worker/src/main.ts` 里被创建并交给发布循环，而 `register()` 在**全仓库无一处调用**。围绕它的机械件——收件箱去重、指数退避重试、十次后进死信、发布者中途死亡后的可见性超时回收——读起来全是「可靠投递」，而它可靠地投递给了零个消费者：每一条事件都被认领、分发给无人、然后标记为 **Published**，一个读起来像「已送达并已被处理」的词。**后果落在退出研究上**：`withdrawParticipant` 发出 `ParticipantWithdrawn`「供下游传播」，而 `enrolment_state` 在 M05 之外**没有任何地方读**——参与者上午退出研究，下午仍可以把一次干预交付记到他的入组上，而协调员屏上写着「退出会停止后续数据采集并传播到相关记录」。**我自己两个增量前做的交付屏，正是把这条假话变成浏览器里点得到的动作的那一步**（在它之前根本没有任何界面能记录一次交付）。**裁定不是去写消费者**：同意撤回一直是对的，靠的不是传播而是**使用时刻求值**——权限引擎在每一次判定时重新读 `consent_current`。这是这个平台真正具备的机制，它不会过期，也不依赖谁哪天把消费者补上。因此把同一条机制铺到唯一两处会把数据挂到入组上的写入（`intervention_sessions`、`assessment_records`，全库仅此两张表带 `enrolment_id`）：入组处于 `Withdrawn`／`Discontinued` 时拒绝写入。**M08 只拒绝答案、不拒绝整行**：`missingnessReason` 本就有 `Withdrawn` 一项，Doc 19 的规矩是缺失必须显式且带类型、不得沉默；整表封死会把本可说明原因的行变成空白，正好是那套词表要防的事。**措辞随之改窄**：对话框不再说传播，改说「从此刻起不能再往这条入组上记任何东西」并明说「**它不会回溯**」，播报也从「传播已开始」改成同一句窄的话。**顺带修掉一个同类缺陷**：`run()` 先播报结果再重新拉列表，而拉列表自己播报「入组列表已更新」——于是任何一次操作的结果播报都在被读到之前就被覆盖掉了，协调员确认退出后，屏上只剩「列表已更新」。**解锁条件**：真要有传播，就得先注册消费者；`apps/worker/test/outbox-consumers.test.ts` 里的绊线会在那一刻失败，提醒先回来改本条与退出措辞，而不是事后 | 实现推导，2026-08-06 |
| D-53 | 「需确认」这一层由 api 客户端替人确认：五个动作一点即成 | **补上五处真正的确认对话框；匹配那块的措辞刻意不写「随时可以关掉」；已实施（2026-08-06）** | 权限引擎有一层动作必须由人确认才放行（`confirmationRequired`，共 45 个），命令把 `confirmed` 透传给引擎，缺了就拒。两个 api 客户端在**每一处调用点都写死 `confirmed: true`**——只要屏上先问过，这只是如实转述；**没问过的地方，这个常量本身就是那句确认**，于是这一层什么也没拦住。逐条核对 45 个动作后，有五处是一点即成：**批准干预版本**（同时属于强认证层，而该组件自己的注释一直写着「批准需要强认证与一次确认」——它只有前者）、**记录资格判定**（同一块屏上的退出一直会问，而结束某人入组的那个判定不会）、**记录安全事件的处置**、**移动安全事件状态（含移到「已处理」）**、**开启匹配**。其余四十个都问过。**测试能钉住什么、钉不住什么，写在测试文件里**：能机械判定「一块发送确认的屏根本没有任何询问手段」（安全事件屏就是这样被抓到的），**不能**判定某一次具体调用是否走在那道门后面——五处里有三处所在的屏本来就有确认框，只是那框是为另一个动作写的。所以真正承重的是每块屏各自的行为测试（五处各补一条），再加一张「哪些屏会发送确认」的清单，新增配对必须由人过一遍而不是悄悄出现。**匹配确认框里刻意缺一句**：不写「你随时可以关掉」——`activateMatchPreference` 只会插入一行 `Active`，全平台没有任何命令把它改成别的；撤回「认识新朋友」同意也只挡住再次开启，因为候选生成读的是偏好行而不是同意。两句都会是平台兑现不了的承诺，所以在能兑现之前一句都不写 | 实现推导，2026-08-06 |
| D-54 | 匹配是一扇单向门：开得了、关不上，撤回同意也不管用 | **补上关闭命令（不受 `open-matching` 同意门控）＋候选生成改为在使用时刻重读同意；已实施（2026-08-06）** | 写 D-53 那个确认框时想写一句「你随时可以关掉」，去核对才发现写不了：`activateMatchPreference` 只会插入一行 `preference_state = 'Active'`，**全平台没有任何命令把这一列改成别的**——屏上提供「开启匹配」，而平台没有那个开关。更糟的是**撤回同意也关不掉**：`matching.activate` 要 `open-matching` 同意，但候选生成 `generateMatchCandidate` 只读偏好行、不读同意，于是撤回「认识新朋友」的人仍然留在池子里、继续被推荐给别人——那正是那块同意屏声称自己是的那个控制（D-2/D-5 的规矩：不提供记录了决定而没人读的控件）。**两处都改**：（一）新增 `matching.deactivate`，把偏好置为 `Inactive`（这个词表从第一版迁移就在 CHECK 里，从来没有代码写过）；**刻意不受 `open-matching` 门控**——进池子要那份同意，出池子不能也要，否则撤回同意就会把人锁在它带进去的东西里，与 D-26「退出社区不带同意前置」同一条理由。关掉已经关掉的，不报错也不写审计：对一个要求离开的人说「你本来就不在里面」，比直接让他在外面更差。（二）`generateMatchCandidate` 在生成前重读 `consent_current`，接受 `Granted`／`Restricted` 且未过期——与权限引擎对同意的判读一致。这是 D-52 那条机制的又一次应用：使用时刻求值，不依赖任何传播。**措辞随之可以写了**：开启确认框现在说得出「你随时可以关掉」，关闭确认框说明停止被推荐、已有的会话不受影响、也不会通知任何人 | 实现推导，2026-08-06 |
| D-55 | 关系上的「允许做什么」是不受校验的自由文本，而它会原样印在参与者的授权屏上 | **写入时按目录校验：关系只能承载引擎真会为它查询的那几个动作；已实施（2026-08-06）** | `proposeRelationship` 把 `permittedActions` 原样写进行里，**不作任何校验**；而权限引擎第 4 步只会在动作的 requirement 带 `requiresRelationship` 时去读关系的这张清单（全平台仅三个：`participant.view-shared`、`relationship.message`、`life-story.contribute`）。写进去的其他任何字符串都没有任何地方读——**而它不是内部账目**：参与者屏（WhoHasAccess）与支持者屏（SupporterApp）都把这张清单逐条印出来，标题是「**这会让他们能做什么**」，且在没有对应措辞时**回退为原始字符串**。也就是说，平台里唯一完全属于参与者自己的那个决定，可以建立在一张平台既不理解、也永远不会兑现的清单上，措辞还由提议方任写。（工作人员屏本来就只提供那三个真实项，所以这条不拒绝任何正当写入；它补的是那块屏与它背后那条毫无校验的路由之间的缝。）**做法**：可授权动作集从 requirement 表**推导**而非另抄一份（`RELATIONSHIP_AUTHORISABLE_ACTIONS`），新增带 `requiresRelationship` 的动作当天即生效，手抄的清单则一定会和引擎漂开。**两块屏的回退也改了**：不再印原始动作名，而是明说「记录为『X』，本平台不认识它，它不授予任何东西」——仍然显示原文，因为把记录在案的东西藏起来会让参与者连问都无从问起，但绝不把它显示成一项权限 | 实现推导，2026-08-06 |
| D-56 | 权限目录声明并授予了十个动作，而没有任何命令检查它们 | **保留但逐条写明理由；用一条双向绊线钉住这张清单；已实施（2026-08-06）** | 没人检查的权限不授予任何东西，所以这十条**都不是入口**。它们是一句**声明**：这份目录是平台对「某个角色可以做什么」的正式陈述，也确实被这样读——决定给谁哪个角色的人在读，事后审计某个角色本可以做什么的人也在读。而这十条描述的是平台并不具备的能力：一个批不了项目的研究批准人（`project.approve`／`project.activate`——`project_state` 没有任何写入方，也没有任何命令能移动它）、一个没人建过分诊步骤的审核员（`moderation.triage`，只有 `moderation.decide`）、一条从未写过的生命故事导出命令（`life-story.export`，参与者实际走 M14 的 `participant.export`）、一个不存在的协议「复核」步（`protocol.review`）、一个没有任何配置界面的 `system.configure`、以及**根本没有任何工作人员读取参与者档案的路径**（`participant.view-assigned`——工作人员屏读的是入组）。`evidence-snapshot.create` 略有不同：快照确实会写，但只在批准证据决定的同一个事务里（ADR-044），单独可创建的快照反而会破坏那条规矩。**两条是刻意的**：`safety-signal.record` 必须永远不检查——有人报告安全隐患时，绝不因为没有角色而被拒（`recordSafetySignal` 定义处已写明）；`participant.view-shared` 由 D-39 裁定为惰性，且指名它的屏幕都照实说了。**不删而是逐条写明理由**：删掉会丢掉设计意图。**绊线是相等断言而非包含断言**，从模块与路由源码推导同一个集合，因此两个方向都会失败——新出现一条没人检查的动作不能悄悄混进来，而真去实现其中某一条时，测试会一直失败到它被从清单里划掉为止 | 实现推导，2026-08-06 |
| D-57 | 问责记录屏说「只有改变了什么的动作才写在这里」，读起来像是「凡改变了什么的都在这里」——而三十九条命令改了东西却什么也没记 | **改正措辞；先补上交付与评估两条；其余用一条相等断言的绊线钉住；已实施（2026-08-06）** | 审计屏在读第一行之前就说清了这份记录**不**包含什么——不记录读取、不记录被拒——两条都属实。紧接着的那句「**只有改变了什么的动作才写在这里**」却被读成「凡改变了什么的都在这里」，而这不成立：**39 条应用层命令写了数据库、一条审计都没记**，其中包括**唯二把数据挂到参与者入组上的那两条**（M07 交付、M08 评估）。一个人问「关于我都记了些什么」，得到的会是一份少了这些的记录，而且没有任何迹象表明少了。**把不完整的记录当作完整的呈现，比没有记录更坏**——因为其中的空白会被读成证据。**本轮先补两条**（交付与评估，理由：它们是参与者自己的问责记录最该有的东西），**其余 37 条逐条列进测试里**，断言是**相等而非包含**，两个方向都会失败：新出现一条不记审计的命令混不进来，而真去补其中某一条时，测试会一直失败到它被划掉为止。**屏上措辞随之改窄**：明说这不是全部改动、这份记录里的空白不是「什么都没发生」的证据、也不能据以下任何结论。测试还把这句话从组件源码里读出来核对，所以清单清空的那天，那句话必须跟着改，测试会告诉你 | 实现推导，2026-08-06 |
| D-58 | 上传的字节没有任何地方可去：`simulated_blobs` 是本平台自己 Postgres 里的一个 `bytea` 列 | **对象存储供应商裁定为 Cloudflare R2（所有者裁定，2026-08-07，与我的建议相反）；先落存储端口与适配器，界面仍不做；已实施** | 隔离管道的每一部分都是真的——放行闸由 CHECK 约束强制，扫描失败永远不可能得出 Available——而底下参与者的照片会进一个数据库列，建表迁移自己就写着「blob store is a deterministic simulator (real vendor Pending External Approval)」。**我的建议是 GCS**：平台已经在 GCP 上、已有 WIF，用 GCS 不需要任何长期凭据、也不需要第二家个人数据处理方，且 us-east1 恰在 always-free 覆盖区内。**所有者裁定用 R2**；代价是一对长期密钥要进 Secret Manager、以及多一份数据处理关系要写进治理文件，这两项由所有者承担，我按裁定实施。**落地形状**：新增 `BlobStore` 端口，两个适配器（Postgres 模拟器、R2 走 S3 兼容 API），管道的三处触点全部改走端口。**两条规则**：（一）**先写字节、后写记录；先写记录、后删字节**——这样任何一处失败留下的都是「没有记录指向的字节」（不可达、标识符不透明、可回收），而不是「记录指向不存在的字节」（平台声称持有一份它并不持有的文件）；（二）**半配置是启动失败，绝不回退**——四个设置全有则用 R2，全无则用模拟器，介于两者之间直接拒绝启动。少一个环境变量就把参与者的文件悄悄写进数据库列，正是这个平台存在的意义所要拒绝的那类沉默的错答案；部署工作流里同一条规则由测试从 `deploy.yml` 读出来核对，因为最可能破坏它的地方是那个自己拼装参数的脚本。**界面仍然不做**：D-50 的另一半没有变——`assignObject` 把归属写在对象一侧，生命故事里没有任何指回对象的引用，也没有按归属资源列出对象的查询，所以只做上传仍然是让人把照片交给一个再也拿不回来的地方。**「已查毒」也仍然不能说**：换存储不改变扫描器只认得 EICAR 测试串这件事；要让那句话成立需要真扫描器（GCP 无托管杀毒产品，通行做法是 Cloud Run 上跑 ClamAV），那是独立的一个增量 | 所有者裁定 + 实现推导，2026-08-07 |
| D-59 | ADR 登记表与实现文档在供应商状态上已经漂开，而扫描器供应商根本没有编号 | **回改七份实现层文档；给扫描器补 ADR-126；R2 的司法辖区作为新待决值挂到 ADR-119；已实施（2026-08-07）** | D-58 落地后我把裁定记进了 `DESIGN_DECISIONS`，却没有回改 ADR 登记表——于是 `IMPLEMENTATION_DECISIONS` 的 ADR-106 仍写着「本地 MinIO；Proposed」，另外六份文档也仍把对象存储列在「未选定供应商」里。**这正是本项目一直在清理的那类缺陷，出现在我自己刚落的增量上。** 核查时又发现三件事：（一）**恶意软件扫描器从来没有 ADR 编号**——其余每个供应商决策都有编号与状态，唯独它没有，因此它不会出现在任何「还差什么」的汇总里，而它恰恰是挡住「已查毒」措辞的那一个；补为 **ADR-126**。（二）**选定 R2 反而新增了一个待决值**：桶的司法辖区——此前平台只有 GCP 一个落地面，现在多一个需要单独确定并写进治理文件的位置；挂到 ADR-119。（三）`RESEARCH_BASELINE` 把 **Knowledge Platform 列为模拟器，而它早已是真实接入**（ADR-110，活的 MCP 端点），同一句里的测试数（约 240）、迁移数（20）、traceability 条数（52）也全部过时（实为 576／26／124）。**措辞守一条线**：所有文档一律写「供应商已定、**尚未接通**」——凭据未配置、当前仍跑模拟器、首次真实往返未验证。把「已裁定」写成「已接入」，与这个平台拒绝的其他假话是同一种 | 实现推导，2026-08-07 |
| D-60 | 平台不肯说自己把文件放在哪里 | **`/ready` 报告存储种类；API 启动时打日志；部署冒烟核对服务自己的回答；已实施（2026-08-07）** | 所有者配好 R2 之后，我要回答「它现在真的在用对象存储吗」，却发现**没有任何办法从外面看出来**：worker 启动时会打一行日志说明选了哪个存储，而**真正处理上传的 API 进程是沉默的**，于是唯一的判断途径是去读部署日志并选择相信它。**一个说不出自己把人的文件放在哪里的平台，是没法核对的平台。** `/ready` 现在报告 `fileStorage`，**只报种类不报桶名与供应商**——这个端点在访问令牌闸之外；也**只报配置结果、不做实时探测**——一个未认证端点按需向对象存储发请求，就是一个放大器，而且是要付费的那种。**部署冒烟改为核对服务自己的回答，而不是核对本次工作流挂了什么**：两者可能不一致（设置对了，但跑着的仍是旧修订版），挂载成功不等于生效。因此现在双向断言——挂了 R2 而服务说是模拟器，或没挂却说是对象存储，都直接失败 | 实现推导，2026-08-07 |
| D-61 | `/ready` 在所有对外部署里返回的是网页外壳，不是就绪判断 | **把 SPA 兜底的排除清单改为可测的单一来源；补上「部署形态」的端到端测试；已实施（2026-08-07）** | 追查存储上报为什么读不到时发现的：单服务部署里 API 同时托管网页，未匹配的 GET 兜底返回 SPA 外壳，而那张手写的排除清单只有 `/v1` 与 `/health`——**没有 `/ready`**。于是**每一个对外部署的 `/ready` 都返回 200 + 一张 HTML 页面**。**一个无论数据库如何都答 200 的就绪端点，比没有更坏**：它报告的是没人检查过的就绪，而盯着它的探针、负载均衡器或人，会因为一个静态文件存在就认为平台正常。**测试没抓到，是因为它测的是部署没有的形态**：e2e 套件不配置网页目录，所以那个把 `/ready` 吃掉的兜底在测试里根本不存在。现已（一）把清单提成 `NON_SPA_PREFIXES` 单一来源，并**从 health 控制器源码读出它声明的每条路由**逐一断言不会被兜底吃掉——手写清单漂移正是本条的成因；（二）按整段匹配而非字符串前缀（`/readiness-dashboard` 不应被 `/ready` 捕获）；（三）补一条**按部署形态启动**（配置网页目录 + 兜底中间件）的端到端测试，断言 `/ready` 仍返回 JSON 而不是外壳。**顺带修掉一处我自己一小时前写的同类缺陷**：`/ready` 的未就绪分支把 `fileStorage` 塞进异常里，而错误过滤器会用平台统一错误信封替换任何 HttpException 的响应体——那个字段写了永远读不到 | 实现推导，2026-08-07 |
| D-62 | 上传完的文件看不回来：归属只记在对象那一侧 | **先落读取路径（按归属资源列出对象）＋新增 `object.view-own` 读权限；界面留给下一个增量；已实施（2026-08-07）** | D-50 拒绝做上传界面的第二条理由，就是这个：`releaseObject` 把归属写在**对象那一侧**（`owning_resource_type`／`owning_resource_id`），而生命故事表里没有任何指回对象的引用，唯一的查询是「按 objectId 查单个」。于是一张照片可以被上传、过检、挂到某条生命故事上，**那条生命故事永远显示不出它**——除非本人记住了一串不透明标识符。现在补上反方向的问题：按归属资源列出对象。**只列 Available**：任何未过隔离的状态都还不能交还，列出来就是展示一份平台并不准备交出的文件。**owner-scoped，刻意如此**：附件属于上传它的参与者，支持者与工作人员在这里读不到任何东西——D-39 已裁定「与支持者共享」在本平台根本不存在，一个假装能共享的列表就是这个项目一直在拆掉的那种空控件。**新增 `object.view-own` 而不是复用 `object.assign`**：原来 `loadOwnedObject` 用「挂载」这个写动作去门控读取，写动作替读取站岗，会让审计记录说错这个人做了什么。**归属资源写在查询参数而不是路径里**：M16 不知道生命故事是什么，为每种归属类型开一条路由，等于把这份知识放进错误的模块。**界面仍未做，这是两个增量里的第一个**：D-50 要求上传界面必须与读取路径一起可用，下一个增量把「在生命故事条目上附一张照片、并在同一条目上看到它」做完；在那之前屏上仍不得出现「已查毒」（ADR-126） | 实现推导，2026-08-07 |
| D-63 | 附件要三步、第三步还得等后台扫描，等于让参与者替平台记账 | **上传时就说明去处，扫描干净即自动挂载；生命故事屏可加照片并看到已挂载的；已实施（2026-08-07）** | 挂一张照片此前是三次调用：建对象、传字节、再 release——而 release 需要一次干净扫描，扫描只由 worker 清扫产生（`*/5 * * * *`，且服务缩容到零时只在有实例时跑）。于是参与者传完照片，要等一段不知道多久的时间，再回来按第二个按钮才算挂上。**这是要一位长者替平台记账、还要知道后台作业跑没跑。** 现在去处写在启动上传的那次请求里（新增 `intended_owning_resource_*` 两列，可空、且两者同有同无的 CHECK），干净扫描时由平台自己完成挂载。**放行闸一点没松**：仍要扫描干净，表上的 CHECK 仍拒绝没有分级、没有归属资源的 Available；分级仍由资源类型推出，与显式路径同一套。**不合法的去处在上传前就拒**——等字节都传完再说「这个类型不能挂」，是把本可以先说的话拖到最坏的时机。**屏上措辞的边界**：说「已收到、正在检查、**还没在这条记录上**」；**不说「已查毒」**（扫描器只认 EICAR 测试串，ADR-126）；并明说别人看不到——这个平台没有任何办法把照片分享给任何人，连支持者也不行（D-39）。**两条自查**：（一）D-57 的审计绊线如期失败——扫描因自动挂载而有了审计条目，清单不划掉就不放行，这正是「相等断言」那半边的用处；（二）列附件的路由不给资源标识时返回 200 空列表，读起来像「这条记录没有附件」，已改为 400 | 实现推导，2026-08-07 |
| D-64 | 照片加得上、拿不回来 | **补上删除（属主限定、需确认、不可撤销）；字节删掉、行留下；已实施（2026-08-07）** | `object_state` 从第一版迁移就允许 `'Deleted'`，而**没有任何代码写过它**——参与者可以往自己的生命故事上挂一张照片，然后**永远拿不下来**。这与 D-54 在匹配上发现的是同一扇单向门，只是这次挂着的是一个人生活里的照片；而**让它变得可达的正是上一个增量**——在加照片的屏出现之前，没有任何人能走到这一步。**字节删掉、行留下**：留下来的那一行说「曾经加过一份文件、后来removed了」，且不含任何照片。把行也删掉等于抹掉「发生过什么」这件事本身，而一个人记录上发生过什么，不是平台可以悄悄改写的。**顺序照旧**：先提交记录、再删字节——失败留下的是没有记录指向的字节，而不是指向已消失字节的记录。**属主限定 + 需确认**：销毁别人拿不回来的东西，不该是一次点击就能做的事；重复删除不报错，对一个已经要求删掉它的人说「它本来就没了」毫无用处。**措辞**：明说照片本身删了、谁也拿不回来，同时明说「你的条目和你写下的一切都没动」——否则「删除」二字会被读成把整条记录一起删了。**两条绊线如期响**：D-53 的确认清单出现新配对（本屏 → removeFile）不放行，逼人去看这块屏是否真的先问；D-57 的审计清单不受影响（新命令自带审计） | 实现推导，2026-08-07 |
| D-65 | 撤回一条生命故事，等于把挂在它上面的照片一起藏起来、且再也删不掉 | **撤回后照片仍然可见、仍然可删；但不能再加；服务端同样拒绝；已实施（2026-08-07）** | 屏上对撤回的说法是「这是私密的、别人碰不到，**它还在这里给你看**」。而我上一个增量把附件区整块写成「非撤回才显示」——于是撤回一条记录，挂在它上面的照片**从屏上消失**，而删除按钮就在那块区域里，**照片同时变得再也删不掉**。也就是说：刚刚为「照片拿不回来」补上的那扇门，对每一个选择把记录设为私密的人又关上了一次，而受影响的恰好是那些明确表达过「我要它更私密」的人。**现在撤回后照片照常列出、照常可删**；**不能再加**——撤回的记录拒绝其他一切改动（不能修订、不能确认证言），屏上也从没说过它还能接受新东西。**服务端同样拒绝**，在 `initiateUpload` 里读生命故事条目的状态（与 M07／M08 读入组状态同一条机制，D-52：使用时刻求值，不依赖传播），并且**条目不存在也拒**——把照片交给一个不存在的地方，就是让它去无处可去。措辞照实说：「已经在上面的会留着，你仍然可以把它们删掉。」 | 实现推导，2026-08-07 |
| D-66 | 参与者的登录页上摆着「工作人员工作区」「支持者工作区」两道不属于他的门 | **按域名分开两个入口（所有者裁定 2026-08-08：同一服务、两个域名）；参与者入口不再出现别人的门；已实施** | 此前工作区是由登录页上的一个按钮选的——一位第一次打开这个平台的长者，面前摆着三道门，其中两道不是他的。**按下去什么也拿不到**（每一条数据路径都由服务端的权限引擎判定），但把别人的工作区摆在一位长者面前，本身就不是一件中性的事。现在由地址决定：`STAFF_HOSTS` 里列出的主机名服务工作人员工作区，其余服务参与者工作区；工作人员一侧也不再显示「回到参与者登录」——那边没有参与者工作区可回，一个把人带去无处的控件比没有更坏。**这一条必须写清楚：它不是访问控制，任何地方都不得这样描述。** 两个域名是同一个部署、同一个共享访问令牌，而身份仍是开发桩（`x-actor-id` 说是谁就是谁，ADR-104）——**能到一边的人就能到另一边，并且在任何一边都可以声称任意身份。** 它给出的是：参与者入口上只剩属于他自己的那道门，以及浏览器按域名分开存放令牌。保护工作人员工作的仍然只有权限引擎。**测试刻意不断言「参与者到不了工作人员界面」**——那会是在断言一件不成立的事。要成为真实边界，需要两个各自持独立令牌的服务（或工作人员侧 IAM-only），本次未做。**主机名按整体匹配**，不做前缀/后缀匹配：`admin.example.org.attacker.test` 不得被认成工作人员地址。**未配置时保持单地址、保留所有入口**——否则本地开发根本进不去工作人员工作区 | 所有者裁定 + 实现推导，2026-08-08 |

## 未随本次裁定解决、仍然开放的

- 「治理记录保留」的确切口径须与实际保留策略一致（ADR-120 未定值，PARTICIPANT C8）
- 申诉期限、既往处置窗口、独立复核人定义、安全事件受控词表（MODERATION U4–U13）
- 数据集定义、解释记录、报告版本三者的状态词表里没有任何拒绝状态（D-25），补上要改词表而不只是加列：是加 `Rejected`，还是像证据评审那样加「退回修改」，取决于被拒之后那件东西还能不能改
- 参与者导航若要恢复到 PARTICIPANT §0.1 的 9 项目的地，移动端需要分层方案（底部四项 + 二级面板，还是移动端与桌面端目的地集合不同）。D-10 只裁定了当前的四项，没有裁定分层方式；A1.4 在移动端因此阻塞

以上未决项**不阻塞 P0 实施**（设计系统基座 + 已实现的 19 屏），但阻塞其所属界面的实现。
